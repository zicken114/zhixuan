# AI Research Assistant 产品功能规格书（开发顺序版）

> **文档类型**：按开发顺序编排的产品功能规格  
> **编排原则**：章节顺序 = 代码开发顺序。AI按此文档从头至尾依次实现，每一章都建立在前面所有章节已完成的基础之上，不存在"后面实现的代码需要回头修改前面代码"的情况。  
> **面向读者**：AI Coding Agent（开发执行者）

---

## 阅读指南

本文档的章节顺序就是代码的开发顺序。请严格按照以下规则执行：

1. **按Phase顺序开发**。完成Phase 0的全部内容后，再进入Phase 1。Phase 1完成后进入Phase 2，以此类推。
2. **Phase内部按子章节顺序开发**。Phase 1中先做功能四，再做功能十一，最后做功能一。
3. **每个功能开发完成后，检查其与已完成功能的衔接是否通畅**，然后再进入下一个功能。
4. **Phase完成状态段落是验收标准**。达到该段落描述的状态，才算该Phase真正完成。

---

# Phase 0：前置基础设施（约1周）

## 本Phase目标

搭建两个**没有用户可见界面**的底层服务。它们本身不是功能，但后续几乎所有功能都依赖它们。先搭建它们，后续开发时可以直接调用，避免回头补洞。

## 前置条件

- 你已完成的产品（悬浮球、AI聊天、Alt+Q快捷菜单、Alt+S截图提取、结果窗口）正常运行
- Tauri v2 + Vue 3 + Rust 技术栈已搭建完毕
- SQLite数据库已接入（tauri-plugin-sql）

---

## 0.1 窗口活动检测框架

### 功能定义

建立一个持续运行在Rust后台的"窗口侦察兵"，定期探测操作系统当前的活动窗口信息（进程名、窗口标题、窗口句柄），判断用户正在使用什么类型的应用——是在Word里写作、在PDF阅读器里读论文、在浏览器里用Overleaf，还是在写代码。

这个服务本身不产生任何用户界面，它的产出是一个可供其他功能调用的结构化信息对象。窗口检测的频率控制在每3秒一次，这个频率足以捕捉用户场景切换，同时CPU开销极低（单次探测耗时通常小于1毫秒）。

### 界面位置

**无用户界面。** 这是一个纯后台服务。它的产出通过Rust的内部状态管理机制（AppState）暴露给其他Command调用。前端Vue代码中没有任何地方直接展示窗口检测结果。

但有一个间接的界面露出点：当Phase 3的写作伴侣和阅读伴侣上线后，Alt+Q快捷菜单的内容会根据窗口检测结果动态变化。用户看到的只是"菜单变了"，看不到背后的检测逻辑。

### 交互流程

这个服务没有用户交互流程。它的"消费者"是后续的功能模块，交互流程在那些功能中描述。这里只描述服务自身的工作流程：

系统启动后，Rust后端创建一个后台定时任务（tokio::time::interval，间隔3秒）。每次触发时：

1. 通过操作系统API获取当前活动窗口的进程名和窗口标题
2. 用一组已知的进程名白名单判断应用类型：
   - winword.exe / soffice → 写作应用（Word/LibreOffice）
   - typora.exe → 写作应用（Typora）
   - code.exe → 代码编辑器（VS Code，需进一步检测是否编辑.tex文件）
   - SumatraPDF.exe / AcroRd32.exe / FoxitReader.exe → PDF阅读器
   - zotero.exe → Zotero（内置PDF阅读器）
   - chrome.exe / msedge.exe / firefox.exe → 浏览器（需进一步检测URL判断是否在线编辑器）
3. 将检测结果（应用名、窗口标题、推测的文档类型）写入AppState的一个共享字段
4. 如果检测结果发生变化（用户切换了应用），触发一个内部事件通知所有订阅者

对于浏览器窗口的进一步判断：如果进程名是chrome/edge/firefox，系统尝试读取当前活动标签页的标题或URL。如果包含"overleaf"，则标记为"在线LaTeX编辑器"。这一步骤在某些平台上可能因浏览器安全限制而无法实现，此时回退到"未知浏览器"标记。

### 实现方案

**Windows平台实现**：使用windows-sys crate调用Win32 API。GetForegroundWindow获取当前活动窗口句柄，GetWindowTextW读取窗口标题，GetWindowThreadProcessId获取进程ID，然后用OpenProcess和QueryFullProcessImageName获取进程名。对于PDF阅读器的文件路径探测，如果窗口标题无法提供完整路径，可以通过NtQuerySystemInformation枚举进程打开的文件句柄来反推。

**macOS平台实现**：使用Accessibility API（AXUIElementCreateSystemWide + AXUIElementCopyAttributeValue读取kAXFocusedApplicationAttribute和kAXTitleAttribute）。macOS的Accessibility API天然能提供当前焦点应用的详细信息，包括文档路径。需要在Info.plist中声明NSAccessibilityUsageDescription权限。

**Linux平台实现**：通过X11的XGetInputFocus或Wayland的对应协议获取活动窗口。X11环境下使用libxdo或xprop命令获取窗口属性。Linux的支持优先级最低，可以先实现X11基础的检测，Wayland支持后续补充。

**跨平台抽象层**：在Rust中定义一个WindowInfo结构体（包含进程名、窗口标题、应用类型枚举、文档路径可选字段），以及一个WindowDetector trait。为每个平台实现这个trait的detect_active_window方法。AppState中持有一个Box<dyn WindowDetector>，在启动时根据编译目标初始化对应平台的实现。

**数据结构设计**：检测结果缓存在内存中（Arc<RwLock<WindowInfo>>），其他功能通过读取这个共享缓存来获取当前窗口状态，不需要每次都重新探测。窗口切换事件通过Tauri的Event系统广播给前端（event名：window:activity-changed），前端有需要响应窗口变化的组件可以监听这个事件。

### 与现有系统的衔接

- **与悬浮球的衔接**：悬浮球本身不需要改动。但Phase 3中，PopupWindow.vue会监听window:activity-changed事件，根据新的应用类型动态渲染不同的菜单内容。
- **与Phase 1项目系统的衔接**：窗口检测服务在AppState中初始化，与项目服务、AI路由服务并列。它不依赖任何其他服务，是AppState中最先初始化的组件之一。
- **与Phase 3写作伴侣/阅读伴侣的衔接**：这两个功能是窗口检测服务的主要消费者。它们通过调用AppState中的window_detector.get_active_info()来获取当前窗口信息，然后决定自己的行为逻辑。

### 为什么先做它

写作伴侣需要知道"用户是否在Word中"来决定是否显示写作菜单。阅读伴侣需要知道"用户是否在PDF阅读器中"来决定是否激活阅读模式。如果先做了写作伴侣再做窗口检测，写作伴侣的菜单切换逻辑会是一个空壳——检测不到应用类型，菜单永远显示默认项。先做窗口检测，后做写作/阅读伴侣，后者可以直接调用前者提供的数据。

---

## 0.2 事件采集系统

### 功能定义

建立一个统一的用户行为事件记录通道。系统中任何功能模块在执行关键操作时，都可以调用这个通道记录一条事件。事件类型覆盖用户科研活动的方方面面：打开应用、发起AI对话、阅读PDF、提取公式、翻译文字、保存到Obsidian、创建实验记录等等。

这个系统的核心价值是"预埋数据管线"。从Phase 0开始，每开发一个新功能，就在该功能的关键操作路径中顺手埋入事件记录代码。等Phase 5开发数据仪表盘时，已经有至少2-3个月的历史数据，可以直接展示有意义的趋势分析。如果等到Phase 5才开始采集事件，仪表盘将是一个空壳。

### 界面位置

**无用户界面。** 事件采集是纯后台行为。用户感受不到自己的操作正在被记录。事件数据存储在用户本地的SQLite数据库中，不上传到任何服务器。

唯一的事件可视化出口在Phase 5的数据仪表盘中。到那时，用户才能看到自己过去几个月的科研活动数据。

### 交互流程

没有直接的交互流程。这里描述的是事件记录的触发规则：

**事件类型定义（20+种）**：
- app_open / app_close — 应用启动/关闭
- chat_start / chat_complete — AI对话开始/完成（记录使用的模型、token数、是否成功）
- pdf_open / pdf_close / pdf_page_change — PDF阅读器打开/关闭/翻页（记录文献ID和页码）
- capture_ocr_start / capture_ocr_complete — 截图OCR开始/完成（记录提取类型）
- clipboard_translate / clipboard_purify / clipboard_format — 剪贴板操作（记录操作类型和内容长度）
- zotero_sync — Zotero同步完成（记录同步的条目数）
- note_save_to_obsidian — 保存笔记到Obsidian
- experiment_snapshot — 创建实验记录
- plugin_invoke — 调用插件处理函数
- sentinel_check — 文献哨兵完成一次检查（记录发现的新论文数）
- synthesis_start / synthesis_complete — 综述生成开始/完成
- team_sync — 团队数据同步

**事件数据结构**：每条事件包含——事件ID（自增）、事件类型、时间戳、所属项目ID（如果有）、持续时长（对于有时间跨度的活动，如阅读、对话）、关联资源ID（如文献ID、会话ID）、额外元数据（JSON格式，存储事件特有的附加信息）。

**批量写入策略**：事件不实时逐条写入数据库（那样I/O开销太大），而是在内存中维护一个环形缓冲区（容量100条）。当缓冲区满、或者距离上次写入已超过30秒、或者应用即将关闭时，批量flush到SQLite。这确保了事件记录的写入性能不影响用户体验。

**隐私控制**：在设置面板中增加一个"无痕模式"开关。开启后，事件采集系统停止记录任何事件。这个开关的状态保存在settings表中，启动时读取。即使开启无痕模式，应用的其他功能不受影响。

### 实现方案

**核心组件**：一个EventCollector单例，持有RingBuffer（容量100条）和一个定时flush任务（30秒间隔）。提供record(event_type, project_id, duration_ms, metadata)方法供其他模块调用。

**数据库表**：创建activity_events表（定义在总体架构的数据库Schema中）。除了基本的索引外，特别注意在event_type、project_id、timestamp三个字段上分别建立独立索引，因为Phase 5的仪表盘查询会频繁按这三个维度筛选。

**事件埋点策略**：Phase 0先建立事件通道和表结构，然后在已有的功能（翻译、OCR、格式化）中也补埋事件记录代码。后续每个新功能开发时，在功能规格书中会明确指出"该功能需要在哪些操作路径中埋入事件记录"。

**埋点位置的具体说明**：
- 在ai_chat_stream函数入口处记录chat_start事件
- 在ai_chat_stream完成（或失败）时记录chat_complete事件，附带上model_used、tokens_in、tokens_out、latency_ms、success、error_type
- 在Alt+Q菜单项的处理函数中记录clipboard_translate/clipboard_purify等事件
- 在Alt+S的capture_region处理完成时记录capture_ocr_complete事件
- 每次窗口检测结果从"非PDF"变为"PDF"时记录pdf_open事件，从"PDF"变为"非PDF"时记录pdf_close事件，并计算持续时长

### 与现有系统的衔接

- **与Phase 1-5所有功能的衔接**：事件采集系统是被所有功能依赖的底层服务。它的接口非常简单——一个record方法——所以其他功能在开发时可以很方便地调用。每个功能的开发规格中会明确指出需要埋哪些事件。
- **与Phase 5数据仪表盘的衔接**：仪表盘的所有数据来源就是这张activity_events表。没有Phase 0的预埋，Phase 5只能展示空图表。
- **与现有代码的衔接**：在lib.rs的AppState中增加event_collector字段。已有的Command处理函数中，在关键位置插入事件记录调用。改动量很小——每个关键操作路径增加一行record调用。

### 为什么先做它

事件采集是"时间敏感型基础设施"。如果等到Phase 5做仪表盘时才想起要采集数据，那么前面Phase 1-4开发的功能都不会有历史记录，仪表盘上线后只能展示Phase 5之后的数据，对用户来说没有价值。从Phase 0就建立事件通道，每开发一个新功能顺手埋点，到Phase 5时已经有至少2-3个月的多维度数据，仪表盘一上线就能给用户惊喜。

---

## Phase 0 完成状态（验收标准）

达到以下状态，Phase 0才算完成：

1. **窗口检测服务**能正确识别用户当前使用的应用类型（写作/PDF/浏览器代码编辑器/其他），检测结果可通过内部API供其他Rust模块调用，应用切换时广播事件通知。
2. **事件采集系统**已创建activity_events表，RingBuffer批量写入机制正常工作，已有的翻译/OCR/格式化功能已补埋事件记录代码，"无痕模式"开关可用。
3. **代码层面**：AppState中新增了window_detector和event_collector两个服务，其他模块可以通过AppState::new()获取它们的引用。
4. **用户感知**：Phase 0完成后，用户感知不到任何变化。这是正常的——Phase 0是纯基础设施，其价值在后续Phase中才会显现。

**Phase 0 → Phase 1 的衔接说明**：进入Phase 1后，项目系统（功能四）在初始化时需要将窗口检测服务和事件采集服务注入到AppState中。AI路由服务（功能十一）和项目服务（功能四）在开发过程中需要在关键路径中调用event_collector.record()。知识库服务（功能一）在索引文献时需要记录kb_index_start/complete事件。

---


# Phase 1：地基三件套（约3-4周）

## 本Phase目标

实现三个被后续几乎所有功能依赖的基础设施。这三个功能上线后，产品从"通用AI助手"质变升级为"有记忆的科研助手"。用户第一次能感受到"AI记住了我的研究"。

## 前置条件

- Phase 0的窗口检测框架和事件采集系统已完成并稳定运行
- 现有功能（悬浮球、AI聊天、Alt+Q、Alt+S、结果窗口）正常运行

---

## 1.1 功能四：科研会话上下文持久化（项目系统）

### 为什么在这个位置做

项目系统被9个后续功能依赖（RAG、哨兵、Zotero桥、写作伴侣、阅读伴侣、实验记录、综述自动化、仪表盘、团队空间）。它是绝对的中心枢纽——所有其他功能都通过project_id与项目关联。如果不先做项目系统，后续每开发一个功能都会遇到"这个功能的数据应该保存在哪个项目下"的问题，被迫回头补项目系统的表结构和API。

### 功能定义

这是整个产品从"通用AI聊天工具"进化为"科研专用工作伴侣"的核心转折。用户可以创建多个"科研项目"（Research Project），每个项目相当于一个独立的工作空间，拥有自己独立的对话历史、关联的文献文件夹、绑定的Zotero收藏夹、关联的Obsidian Vault目录、以及专属的AI上下文记忆。

当用户切换到某个项目时，AI会自动加载该项目的全部背景——它知道这个项目是关于什么的、用户之前已经讨论过哪些关键问题、还有哪些待办事项没完成。AI的回答因此是连续的、有上下文的，而不是每次从零开始。

### 界面位置

**第一处：悬浮球展开面板顶部新增项目选择器。** 当用户把鼠标悬停在悬浮球上时，展开的面板最顶部出现一个下拉条，显示当前激活的项目名称（比如"毕业论文"），右侧有一个小箭头。点击下拉后显示所有项目列表（每个项目前面有一个彩色圆点），以及"通用会话"选项。列表底部有"+ 新建项目"和"管理项目"入口。整个选择器高度控制在30像素，不占用太多面板空间。

**第二处：主窗口侧边栏全面重构为"项目-会话"双层结构。** 现有侧边栏只显示会话列表。重构后，侧边栏顶部显示当前项目名称和彩色标识，下方分为三个可切换的标签："会话"（该项目下的所有对话历史）、"文献"（该项目关联的知识库文献，与功能一的知识库面板对接）、"待办"（该项目的任务清单）。会话列表中的每个条目显示会话标题、最后活动时间、以及该会话中AI使用的模型名称。

**第三处：主窗口聊天区域顶部新增项目信息栏。** 在标题栏下方、聊天记录上方，增加一个细条区域，左侧显示当前项目名和彩色圆点，中间显示该项目的关键统计（"12篇文献 · 8次对话 · 3个待办"），右侧显示"本周2篇新文献到达"的动态提示（与功能二对接）。这个信息栏可以折叠。

**第四处：首次启动的欢迎面板增加"从项目开始"入口。** 现有的欢迎面板有"Start With Floating Ball"，在旁边增加"创建我的第一个科研项目"按钮，引导新用户从一开始就建立项目化工作习惯。

**第五处：设置面板增加项目关联配置。** 在项目编辑弹窗里，用户可以配置：该项目关联的本地文献文件夹（知识库自动扫描）、关联的Zotero收藏夹（与功能三对接）、关联的Obsidian Vault路径（与功能三对接）、该项目的研究关键词标签。

### 交互流程

**场景A：创建项目**

陈同学双击悬浮球打开主窗口 → 点击顶部项目选择器 → 选择"+ 新建项目" → 弹出简洁的创建对话框 → 输入项目名称"毕业论文：多模态大模型幻觉检测" → 选择项目颜色（蓝色）→ 点击创建 → 系统提示"是否关联文献文件夹？" → 选择"是"并指定他的文献存放目录 → 系统等待Phase 1.3的知识库功能上线后自动开始扫描索引。

项目创建完成后，主窗口自动切换到这个新项目，侧边栏为空（还没有会话），顶部信息栏显示"毕业论文：多模态大模型幻觉检测 · 0篇文献 · 0次对话 · 0个待办"。

**场景B：多项目切换**

陈同学正在"毕业论文"项目下与AI讨论实验设计。此时导师发来消息让他帮忙看看一个横向课题的方案。他点击悬浮球项目选择器 → 切换到"横向课题：遥感图像分割" → 主窗口整体切换：侧边栏显示该项目的所有历史会话（上次讨论是在3天前）、聊天记录区域清空并加载该项目下最后活跃的会话内容、顶部信息栏显示该项目的统计数据。AI的欢迎语变为："欢迎回到横向课题。上次我们讨论了遥感图像分割的基线模型选择，你还没有决定用U-Net还是DeepLab。需要我继续帮你分析吗？"

**场景C：AI主动加载项目上下文**

陈同学在"毕业论文"项目下输入："我应该用什么评估指标？" → 系统在处理这个问题之前，自动执行以下上下文组装：

1. 从该项目的知识库中检索与"评估指标"相关的文献片段，找到3篇相关论文的关键段落（依赖Phase 1.3的知识库）
2. 从该项目的最近5个会话中提取AI之前生成的高价值结论（比如"你之前提到过F1-score不适合不平衡数据集"）
3. 从该项目的待办清单中读取尚未完成的任务（"比较Precision-Recall曲线和ROC曲线的适用场景"）
4. 把这以上内容组装成一段"项目背景信息"，附加在System Prompt的最前面发给AI

AI的回答因此是连续的、有上下文的，而不是每次从零开始。

**场景D：待办管理**

陈同学在对话中对AI说"这个思路很好，我之后要试一下用KL散度作为评估指标" → AI自动识别出这是一个"待办事项"，在回答末尾追加一个可交互的待办卡片"待办：尝试KL散度作为评估指标" → 陈同学点击卡片上的"确认添加"按钮 → 该待办被加入项目待办列表 → 侧边栏"待办"标签上的数字变为1 → 下次陈同学进入这个项目时，AI会在开场白中提醒"你还有一个待办事项没有完成"。

### 实现方案

**项目数据模型**：项目是核心的聚合根实体。每个项目包含基础信息（ID、名称、颜色、创建时间、更新时间）、关联配置（绑定的本地文件夹路径、Zotero收藏夹Key、Obsidian Vault路径、关键词标签数组）、以及动态统计（文献数量、会话数量、待办数量、最后活跃时间）。项目数据存储在SQLite的projects表中。

**会话归属改造**：现有的chat_sessions表需要增加一个project_id外键字段，允许为空（空表示通用会话）。这是Phase 1中最"侵入性"的数据库变更——它要求修改所有查询会话列表的SQL语句，默认按当前活跃项目过滤。但做这个变更的时机必须放在Phase 1的最开头，因为后续所有功能都假设会话归属于某个项目。

**上下文注入器**：这是项目系统的"灵魂"组件。它负责在每次用户发送消息前，自动组装与该项目的背景信息。组装过程分为四个来源，按优先级从高到低排列：

- **来源一：知识库检索**（依赖Phase 1.3完成后才能启用）。将用户的当前提问作为查询，在项目关联的知识库文档中检索最相关的3-5个文本块。检索结果限制总长度不超过1500字，避免挤占AI模型的上下文窗口。在Phase 1.3完成前，这个来源暂时为空。

- **来源二：历史对话摘要**。系统不会把所有历史对话都塞给AI（会导致上下文爆炸），而是维护一个"会话摘要"机制。每次对话结束后，AI自动用一句话总结这个会话的核心结论。这些摘要句存储在chat_messages表的metadata字段中。当用户回到一个项目时，系统读取最近5个会话的摘要，组成"前期讨论要点"。

- **来源三：待办提醒**。从项目的待办表中读取status='pending'的待办事项，最多显示3个高优先级的，格式化为"待办提醒：1.xxx 2.xxx"。

- **来源四：新文献提醒**（依赖Phase 4.1完成后才能启用）。如果该项目的文献哨兵在近期发现了新论文且用户尚未查看，在上下文中追加"本周有N篇新文献到达"的提示。在Phase 4.1完成前，这个来源暂时为空。

这四个来源的内容被拼接成一段结构化的文字，作为System Prompt的一部分附加在对话最前面。AI在回答时就会自然地引用这些背景信息。

**待办的自动提取与手动管理**：待办有两种产生方式。一是AI自动提取——每次对话结束后，系统让AI回顾整个对话，判断用户是否表达了"之后要做某事"的意图。如果有，AI输出待办建议，以可交互卡片形式展示在对话末尾，用户点击确认后才真正加入待办表。二是用户手动添加——在侧边栏的"待办"标签页，用户可以像普通Todo应用一样手动输入待办事项。每个待办有内容、优先级、创建时间、完成状态。完成的待办自动归档，不再显示在AI上下文中。

### 与现有系统的衔接

- **与悬浮球的衔接**：悬浮球的展开面板是用户切换项目的主要入口。项目选择器的设计需要紧凑——悬浮球本身只有60x60，展开面板顶部留出30像素高度作为项目切换条。选择器使用现有的PopupWindow.vue组件扩展，不创建新窗口。
- **与主窗口的衔接**：主窗口的侧边栏从单层会话列表升级为多层结构。这个重构需要调整Pinia状态管理，增加projectStore来管理当前活跃项目。当用户切换项目时，所有子模块（会话列表、知识库文献、待办清单）自动重新加载。项目信息栏嵌入MainWindow.vue的顶部区域。
- **与AI聊天服务的衔接**：aiClient的每次调用都需要传入当前project_id，后端根据project_id自动触发上下文注入。这是完全透明的，前端不需要改动聊天界面的核心逻辑，只需要在调用参数中附加project_id即可。上下文注入发生在Rust后端，在消息发送给AI模型之前。
- **与Phase 0事件采集的衔接**：项目切换时记录project_switch事件（带上源项目ID和目标项目ID）。项目创建时记录project_create事件。待办完成时记录todo_complete事件。

---

## 1.2 功能十一：多模型智能路由

### 为什么紧跟项目系统

从这一刻开始，后续所有需要调用AI的功能（知识库问答、写作润色、综述生成、哨兵分析）都通过路由器发起。越早实现，后续功能开发时越省心——不需要每个功能自己决定用哪个模型，只需要标注任务类型，路由器自动处理。

### 功能定义

这是一个隐藏在后台的"智能调度员"。用户在设置面板中配置了多个AI模型提供商（OpenAI、DeepSeek、Kimi等），不同任务需要不同的模型特性——翻译任务需要速度快、成本低，文献综述需要长上下文支持，公式提取需要视觉理解能力。智能路由系统自动根据当前任务的类型，选择最合适的模型来执行，用户不需要手动切换。如果首选模型调用失败（超时、API限流），系统会自动降级到备选模型。系统还记录每次调用的用量和成本，帮助用户控制开销。

### 界面位置

这个功能大部分是后台运行的，用户几乎看不到它的存在。但它在以下几个地方有 subtle 的露出：

**第一处：AI对话消息气泡的元数据角标。** 每条AI回复的右下角增加一个非常小的文字标签（灰色8号字），显示"via DeepSeek-V3 · 1.2s · ¥0.003"，表示这条回答使用的是哪个模型、响应耗时、预估成本。用户悬停在这个标签上时，弹出详细面板显示完整的路由决策信息（"选择原因：翻译任务偏好低延迟模型 · 首选模型超时，已降级到备选"）。

**第二处：设置面板新增"模型路由"卡片。** 展开后显示当前配置的所有模型profile（每个模型的名称、支持的能力、平均延迟、成本估算），以及一张"任务类型→模型映射表"。用户可以修改默认映射（比如"翻译任务默认用DeepSeek"），也可以为特定项目设置专属映射。

**第三处：主窗口侧边栏底部新增"用量"小面板。** 折叠状态，点击展开后显示今日/本周/本月的模型调用统计：总请求次数、总Token消耗、各模型使用占比、预估总成本。这帮助用户（尤其是自费API的学生）了解自己的花费。

### 交互流程

**场景A：无感知切换**

钱同学正在使用悬浮球。他复制了一段英文摘要，按Alt+Q选择翻译 → 系统后台自动识别这是"translation"任务类型 → 查询路由表，发现翻译任务的首选模型是"DeepSeek-V3"（速度快、成本低、多语言能力强）→ 调用DeepSeek的API → 1.2秒后返回结果 → 结果窗口显示翻译内容，右下角小标签显示"via DeepSeek-V3 · 1.2s · ¥0.003"。

接着钱同学在主窗口中打开"毕业论文"项目，输入"请帮我基于这些文献生成一份5000字的综述" → 系统识别这是"literature_review"任务类型 → 查询路由表，发现综述任务需要长上下文模型，首选是"GPT-4o-128k" → 调用OpenAI的API → 由于综述需要处理大量文献，这次调用消耗了较多Token → 右下角标签显示"via GPT-4o-128k · 45s · ¥0.12"。

然后他按Alt+S截取了一个包含复杂公式的图片，选择"公式提取" → 系统识别这是"extraction+vision"任务，需要视觉模型 → 首选"GPT-4o"（多模态能力强）→ 调用成功 → 标签显示"via GPT-4o(vision) · 8s · ¥0.015"。

整个过程中钱同学从未手动切换过模型，所有选择都是自动完成的。

**场景B：降级处理**

钱同学的综述生成任务调用了GPT-4o-128k，但由于网络波动，请求在15秒后超时。路由系统检测到超时后，自动将请求转发到备选模型"Kimi-k1.5-128k"。这次调用成功。最终用户收到的回答右下角标签显示"via Kimi-k1.5-128k · 38s · ¥0.08（原首选模型超时已降级）"。用户点击标签可以看到完整的降级日志。

**场景C：用量管理**

钱同学月底查看用量面板，发现：本月总调用432次，消耗Token 1.2M，预估成本¥28.5。其中翻译任务占了60%的调用次数但只花了¥3.2（因为翻译用便宜的轻量模型），综述生成只占了10%的调用次数但花了¥18.5（因为用了贵但强的长上下文模型）。他意识到如果减少综述生成的频率，下个月可以省不少钱。

### 实现方案

**模型Profile管理**：系统维护一个模型profile列表，每个profile描述一个可用模型的特性：提供商名称、模型ID、支持的能力标签（文本/视觉/嵌入/长上下文/推理）、最大上下文长度、历史平均延迟（基于该用户的使用记录动态更新，初始值为默认值）、每千Token成本（美元）。用户可以在设置面板中启用或禁用某个profile，也可以新增自定义profile（填写Base URL和模型名）。已有的8个预设提供商自动转换为8个默认profile。

**任务分类器**：当用户发起一个AI请求时，系统首先需要判断这是什么类型的任务。分类依据有三层：第一层是规则引擎快速匹配——如果用户通过Alt+Q选择了"翻译"菜单项，任务类型直接标记为"translation"；如果用户上传了图片并选择了"公式提取"，标记为"extraction+vision"。第二层是上下文推断——如果用户在某个项目下提问，且该项目的知识库检索结果显示涉及大量文献，任务倾向标记为"literature_review"。第三层是轻量AI分类器——对于模糊的输入（比如用户在聊天框里直接打字，没有通过菜单选择），系统把用户输入的前50个字传给一个本地轻量模型（通过Ollama运行2B参数模型），让它判断这属于什么类型的问题。三层分类的结果取置信度最高的。

在Phase 1实现时，第三层分类器可以先不实现（因为本地小模型的调用需要额外配置），只使用前两层规则匹配即可覆盖90%的场景。第三层作为后续优化的TODO项。

**路由决策引擎**：任务类型确定后，系统查询"任务需求表"，获取该任务对模型能力的需求。比如"translation"任务的需求是：文本能力必需、速度偏好fast、成本偏好low、上下文长度short、视觉能力不需要。然后系统在所有可用的模型profile中筛选，排除不满足必需能力的模型，对剩余模型按综合评分排序。评分公式考虑：历史平均延迟（越短分越高）、成本（越低分越高）、本地模型额外加分（因为隐私好且零API费用）、历史成功率（失败率高的模型降权）。选出排名第一的作为首选，排名第二和第三的作为备选。

**降级与重试**：调用首选模型时，系统设置合理的超时时间（翻译5秒、普通聊天15秒、综述生成120秒）。如果超时或收到API错误（429限流、503服务不可用），系统立即用备选模型重新发起请求。降级过程对用户完全透明，只在消息元数据角标中记录降级信息。如果所有备选都失败，系统最终给用户一个友好的错误提示"当前模型服务暂时不可用，请稍后重试"。

**用量统计**：每次AI调用完成后，系统记录一条用量日志（存储在usage_records表中），包含：时间戳、任务类型、项目ID、使用的模型、输入Token数、输出Token数、延迟（毫秒）、是否成功、是否降级、预估成本。这些数据存储在本地SQLite中，定期聚合生成用量报告。预估成本基于模型profile中配置的单价计算。

### 与现有系统的衔接

- **与AI客户端的衔接**：现有的aiClient调用方式基本不变，只需要在调用参数中增加一个可选的task_type字段。后端在收到调用后，由路由器代替直接调用模型。前端界面改动极小——只是在每条AI回复的右下角增加一个极小的模型信息角标。
- **与设置面板的衔接**：设置面板中的AI提供商配置从"单选"模式扩展为"多选+优先级"模式。每个已配置的提供商可以设置：是否启用、默认用于哪些任务类型、作为哪些任务类型的备选。原有的8个预设提供商自动转换为8个模型profile。
- **与项目系统的衔接**：路由决策可以按项目定制。比如"毕业论文"项目下的所有AI调用默认使用长上下文模型，而"通用会话"默认使用轻量模型。这个映射关系存储在项目的配置中。
- **与Phase 0事件采集的衔接**：每次AI调用完成后记录chat_complete事件（带上model_used、tokens_in、tokens_out、latency_ms、cost、success、is_fallback等字段）。

---

## 1.3 功能一：本地知识库与语义检索

### 为什么放在Phase 1最后

知识库依赖项目系统（检索范围按项目隔离），但不依赖多模型路由（向量化用本地Ollama模型，不走在线API路由）。它可以与多模型路由并行开发，但考虑到开发资源聚焦，放在Phase 1末尾更稳妥——先确保项目系统和路由器这两个"指挥中心"就位，再来搭建"数据底座"。

### 功能定义

这是一个在用户本地电脑上建立的"个人学术搜索引擎"。用户指定一个或多个文件夹（比如存放PDF的文件夹、Zotero的数据目录、或者某个课题的资料文件夹），系统后台自动把这些文档的内容读取出来，切成小段，转换成数学向量，存入本地的向量数据库。之后，用户可以用自然语言提问，比如"我之前读过哪些关于对比学习在医学影像里应用的论文？"，系统不是去网上搜，而是在用户自己的文档里找答案，并且能告诉你是哪篇论文的第几页。

这个功能的核心价值是：让AI的回答基于用户自己的文献库，而不是通用训练数据。同时保证敏感论文永远不上传到云端——所有索引和检索都在本地完成。

### 界面位置

**第一处：主窗口侧边栏新增"知识库"标签页。** 与"会话""待办"并列（这些都是Phase 1.1项目系统建立的侧边栏标签体系）。点击后右侧主区域切换为文献管理面板。面板顶部有"添加文件夹"按钮和搜索框，中间是已索引的文献列表（显示标题、页数、索引状态——已完成/处理中/失败），底部显示当前索引任务进度（"正在处理第23/128篇：《论文标题》..."）。

**第二处：设置面板新增"知识库"配置卡片。** 在现有的AI模型设置下方，增加一个可折叠的知识库配置区域。用户在这里指定哪些文件夹需要被监控、索引策略（自动还是手动触发）、选择本地嵌入模型（默认使用Ollama运行的nomic-embed-text或bge-m3模型，也可以切换到在线API如OpenAI text-embedding-3-small）。

**第三处：AI对话时的隐性触发。** 当用户在某个项目内发起对话时，系统会自动先查询知识库，把检索到的相关文献片段悄悄附加到AI的上下文里。这个行为用户看不见过程，但会在AI回答的末尾看到一个"参考来源"折叠面板，里面列出引用到的论文标题和页码。点击引用可以直接打开PDF到对应页面。

### 交互流程

**首次建立知识库的路径：**

用户打开设置面板 → 展开"知识库"卡片 → 点击"添加文献文件夹" → 弹出系统文件选择器 → 选择存放PDF的文件夹（可多选）→ 系统开始后台扫描，主窗口侧边栏的知识库面板自动打开 → 用户看到文件列表逐行出现，每行显示文件名和一个小状态图标（排队中/正在提取/正在向量化/已完成/失败）。

对于较大的PDF（比如扫描版教材），提取可能需要几十秒，用户可以随时离开面板去做别的事，后台任务会继续。完成后系统右下角弹出"已索引128篇文献"的气泡提示。

**日常使用路径：**

用户双击悬浮球打开主窗口 → 确认当前处于某个项目下（比如"毕业论文"）→ 在聊天输入框输入问题 → 发送后AI回答 → 如果回答引用了本地文献，回答底部出现"参考来源：1.《Contrastive Learning of Medical...》第5页" → 用户点击该引用 → 系统尝试用系统默认的PDF阅读器打开该文件并跳转到对应页面。

**主动检索路径：**

用户打开主窗口 → 切换到侧边栏"知识库"标签 → 在顶部搜索框输入关键词或自然语言问题 → 回车后结果以卡片形式展示，每张卡片包含论文标题、相关段落摘要、页码、相关度百分比 → 用户可以点击"在对话中引用"把该卡片插入当前聊天。

### 实现方案

**文本提取环节**：系统需要能够读取多种格式的文件。对于PDF，使用PDF文本提取库将其转为纯文本，同时尽量保留页码信息。对于Markdown和TXT文件直接读取。对于HTML（比如从网页保存的文献页面），先做HTML到文本的净化。整个提取过程是后台异步进行的，每提取完一个文件就进入下一个环节。

有一个特殊挑战：扫描版PDF（图片形式的PDF）没有文本层，需要先OCR再提取。Phase 1实现时，对于这类PDF，系统先标记为"需要OCR"状态，后续Phase中集成视觉模型来处理。Phase 1至少要先支持有文本层的PDF（这覆盖了90%以上的学术论文）。

**文本分块与向量化**：提取出的文本需要被切成小段（因为单篇论文可能有几万字，超过了AI模型的上下文限制）。分块策略采用"智能边界分块"——优先在段落边界处切分，如果段落太长再在句子边界处切分，每段保留一定重叠（比如每段末尾和前段末尾重复20个字），确保语义连续性。每段目标长度控制在300-500个Token。

分块后的每段文字需要转换成一组数字向量（768维或1024维），这个过程叫做"嵌入"。系统默认调用用户本地运行的Ollama服务中的轻量嵌入模型（nomic-embed-text约70MB，bge-m3约500MB）来完成。如果本地Ollama没有运行或没有安装嵌入模型，系统提示用户安装。转换后的向量存入SQLite数据库的扩展模块sqlite-vec。

sqlite-vec是一个SQLite扩展，它为普通SQLite表添加了向量相似度搜索能力。它使用HNSW索引结构，支持高效的近似最近邻搜索。安装方式是将sqlite-vec编译为动态库，然后在Rust中通过rusqlite的load_extension方法加载。

**混合检索**：用户提问时，系统首先把用户的问题也转成向量（使用同一个嵌入模型），然后在向量数据库中搜索最相似的文本块（这叫"语义检索"，能找到字面不完全一样但意思相近的内容）。同时，系统还会对文本块做一个传统的关键词全文检索（SQLite FTS5虚拟表）。最后把两种检索的结果按一定比例混合排序（默认向量相似度占70%、关键词匹配占30%），确保既不错过同义词表达，也不遗漏精确关键词匹配。

检索结果中的论文如果是用户已经读过并做过笔记的，会在结果卡片上额外显示"已读"标签和阅读笔记摘要。

**增量更新与文件监控**：系统在后台维护一个文件系统监控器（使用notify crate），当用户指定的文件夹里有新文件加入、现有文件修改、或者文件被删除时，自动触发增量索引。对于修改的文件，重新计算文件哈希值（SHA256），如果和上次不同就重新提取和向量化。

**项目隔离**：知识库的索引和检索默认按项目隔离。用户在项目A的搜索不会搜到项目B的文献。检索时在SQL查询中加入project_ids数组字段的过滤条件。如果用户没有指定项目（通用会话），则搜索全部文献。

### 与现有系统的衔接

- **与项目系统的衔接**：知识库是项目系统的"数据底座"。每个项目可以关联一个或多个文献文件夹，知识库只索引和检索该项目关联的文件夹中的文献。项目切换时，知识库面板自动重新加载对应项目的文献列表。
- **与AI聊天服务的衔接**：每次用户发送消息前，系统自动调用知识库检索，把检索到的3-5个最相关文献片段附加到System Prompt中。这个行为对前端透明——前端不需要改动聊天界面的核心逻辑。检索只在项目上下文中进行（通用会话则搜索全部文献）。
- **与悬浮球的衔接**：悬浮球本身不需要改动。当知识库正在索引时，悬浮球的小红点可以提示"正在索引N篇文献"。索引完成后提示"知识库已更新"。
- **与Phase 0事件采集的衔接**：知识库索引任务开始和完成时分别记录kb_index_start和kb_index_complete事件。用户在知识库中执行搜索时记录kb_search事件。

---

## Phase 1 完成状态（验收标准）

达到以下状态，Phase 1才算完成：

1. **项目系统**：用户可以创建/编辑/删除/切换项目。每个项目有独立的对话历史、待办清单、关联配置。AI对话会自动加载项目上下文（包含历史对话摘要和待办提醒）。悬浮球展开面板有项目选择器。主窗口侧边栏有"会话""待办"标签。
2. **多模型路由**：系统能根据任务类型自动选择模型（翻译用轻量模型、长文本用上下文模型、视觉用多模态模型）。失败时自动降级。每条AI回复显示模型信息和成本角标。设置面板有模型路由配置。
3. **本地知识库**：用户可以添加文献文件夹，系统自动索引PDF/Markdown/TXT文件。支持自然语言语义检索。检索结果在AI对话中作为引用来源展示。知识库面板显示索引进度和文献列表。
4. **数据层面**：projects表、chat_sessions表（含project_id）、knowledge_docs表、doc_chunks表、chunk_embeddings虚拟表、usage_records表都已创建并正常工作。
5. **用户感知**：用户能明显感受到三个变化——"AI记住了我的研究（项目上下文）""翻译变快了而且显示了用的什么模型""AI的回答引用了我自己的文献"。产品从"通用AI助手"升级为"有记忆的科研助手"。

**Phase 1 → Phase 2 的衔接说明**：进入Phase 2后，Zotero/Obsidian桥需要利用Phase 1.1建立的项目系统（每个项目关联自己的Zotero收藏夹和Obsidian Vault路径）和Phase 1.3建立的知识库（Zotero文献的元数据缓存和全文索引）。项目系统侧边的"文献"标签在Phase 2完成后将同时展示知识库文献和Zotero文献。

---


# Phase 2：工具桥接（约2周）

## 本Phase目标

打通Zotero和Obsidian这两个科研人员最常用的外部工具，为后续Phase 3的高频工作流功能（写作伴侣、阅读伴侣）提供数据通道。没有这一层桥接，写作伴侣无法推荐用户Zotero库中的文献，阅读伴侣无法将笔记保存到Obsidian。

## 前置条件

- Phase 1全部完成：项目系统、多模型路由、本地知识库均已上线并稳定
- 用户已在设置面板中配置了至少一个AI模型提供商

---

## 2.1 功能三：Zotero与Obsidian深度联动桥

### 为什么在这个阶段做

写作伴侣（Phase 3.1）的"智能引用推荐"和阅读伴侣（Phase 3.2）的"保存到Obsidian"都直接依赖这个功能。如果先做了写作伴侣再做Zotero桥，引用推荐功能会是一个空壳——它检测不到用户的文献库，只能给出通用推荐。先做桥接，后做工作流，引用推荐和笔记保存才有真实数据可用。

### 功能定义

这个功能让你的科研助手不再是孤岛，而是成为两个已有科研基础设施——Zotero（文献管理）和Obsidian（笔记管理）的"智能外挂"。

具体来说有三类能力：
- **Zotero联动**：用户可以在悬浮球的快捷菜单中直接搜索自己的Zotero文献库，获取引用推荐，并一键将格式化后的引用插入到正在编辑的Word或Overleaf文档中。
- **Obsidian联动**：用户可以把AI对话中有价值的内容一键保存为Obsidian笔记，系统会自动建立双向链接（比如对话中提到了"扩散模型"，系统会检测到用户Obsidian Vault中已经有一篇叫"扩散模型"的笔记，自动在新笔记中建立[[扩散模型]]链接）。
- **双向数据同步**：用户通过截图OCR提取的公式或表格，可以一键关联到Zotero中对应的文献条目下。

### 界面位置

**第一处：快捷菜单（PopupWindow）新增"智能引用"入口。** 当系统检测到用户当前活动窗口是写作应用时（依赖Phase 0的窗口检测），Alt+Q弹出的快捷菜单除了原有的翻译、净化、格式化之外，新增一个"推荐引用"按钮。这个按钮只在写作应用激活时显示，其他场景下隐藏。

**第二处：快捷菜单新增"保存到Obsidian"入口。** 当用户在主窗口与AI完成一段对话后，关闭按钮旁边增加一个"存到Obsidian"图标按钮。同样，在Alt+Q快捷菜单中也增加"保存选中文字到Obsidian笔记"的选项。

**第三处：设置面板新增"外部工具连接"卡片。** 在现有的AI提供商设置和知识库设置下方，增加两个配置区块：Zotero连接设置（填写Zotero用户ID、选择同步间隔）和Obsidian连接设置（选择Vault路径、默认笔记存放文件夹、模板文件夹）。

**第四处：结果展示窗口（ResultWindow）的引用动作栏。** 当用户使用Alt+S截图提取公式后，结果窗口除了原有的"复制"按钮，增加"关联到Zotero文献"按钮——点击后弹出一个小面板，显示用户最近在读的论文列表（从本地阅读记录和Zotero最近添加条目获取），选择后这条提取记录就与该论文绑定。

**第五处：主窗口AI对话的每条消息气泡右上角增加操作菜单。** 悬停在AI回答的气泡上时，右上角出现"..."菜单，里面有"将本条回答保存为Obsidian笔记"选项。

### 交互流程

**场景A：写作时插入引用**

赵老师在Word中写论文，写到"近年来，Transformer架构在自然语言处理领域取得了突破性进展"，感觉这里需要引用几篇关键论文。她选中这句话，按Alt+Q → 快捷菜单弹出，看到"推荐引用"选项（因为系统检测到了Word是活动窗口，依赖Phase 0的窗口检测）→ 点击"推荐引用" → 结果窗口打开，显示3篇候选论文：

1. "Attention Is All You Need" (Vaswani et al., 2017) — 推荐理由："这是Transformer的开创性论文，最适合作为该领域的起点引用" — 预览引用格式："[1]"
2. "BERT: Pre-training..." — 推荐理由：... — 预览引用格式："[2]"
3. "Language Models are Few-Shot Learners" (GPT-3) — 推荐理由：... — 预览引用格式："[3]"

赵老师勾选第1篇和第3篇，点击"插入引用" → 系统通过系统级集成将"[1,3]"插入Word光标位置（如果无法直接插入，则写入剪贴板并提示"已复制，请粘贴"）→ 同时系统记录这两篇论文被引用的位置，后续可以一键生成参考文献列表。

**场景B：对话转笔记**

钱同学与AI讨论了30分钟"扩散模型在分子设计中的应用"，获得了很多有价值的观点。对话结束后，他在主窗口右上角点击"存到Obsidian"图标 → 弹出模板选择对话框（"摘要版""完整记录版""Q&A版"）→ 选择"摘要版" → 系统自动在Obsidian Vault的指定目录下创建一个新笔记文件，文件名为"2026-04-23 扩散模型在分子设计中的应用.md" → 内容包含：

- 前置元数据（YAML frontmatter）：标题、日期、标签（自动从对话内容提取：扩散模型、分子设计、AI制药）、来源（AI对话会话ID）、所属项目
- 正文结构：研究摘要（AI自动生成的3段话）、关键结论列表、待验证假设列表、完整对话记录的折叠区块
- 双向链接：如果系统检测到对话中提到的"扩散模型"这个关键词在Vault中已经存在同名笔记，则在正文中自动生成"[[扩散模型]]"链接

笔记保存完成后，系统通过Obsidian的URI协议自动在Obsidian中打开这篇新笔记（如果Obsidian正在运行）。

**场景C：提取内容关联文献**

孙同学用Alt+S截取了一篇论文第8页的一个复杂公式，系统提取出LaTeX代码。在结果窗口中，除了"复制"按钮，她点击"关联到Zotero文献" → 弹出最近阅读文献列表（从本地阅读记录和Zotero最近添加条目获取）→ 选择"Attention Is All You Need" → 系统记录"该公式提取自这篇论文的第8页" → 后续在知识库中检索时，这个提取片段会被标记为来源于该文献。

### 实现方案

**Zotero集成技术路径**：Zotero在本地运行时默认开启了一个HTTP JSON API（监听在127.0.0.1:23119）。系统通过这个API与Zotero通信，不需要任何浏览器插件。API支持的操作包括：获取用户的文献条目列表（分页获取，每页100条）、获取收藏夹结构、按关键词搜索条目。

系统启动时会先测试这个端口是否可达（发送一个GET请求到http://127.0.0.1:23119/users/{userId}/items?limit=1）。如果Zotero没有运行，则在设置面板中提示"Zotero未运行，请启动Zotero以启用联动功能"。

为了提高性能，系统在本地SQLite中维护一份Zotero文献的缓存表（zotero_items_cache和zotero_collections_cache），每30分钟自动同步一次增量数据（只同步自上次同步以来有变更的条目，利用Zotero API的since参数）。这份缓存表的结构在总体架构的数据库Schema中已经定义。

当用户需要"智能引用推荐"时，系统会读取当前写作段落的内容，提取其中的技术关键词，然后在这份本地Zotero缓存中搜索标题、摘要或关键词与用户写作内容最匹配的文献。搜索算法使用关键词匹配加权TF-IDF相关性，如果同时开启了知识库功能（Phase 1.3），还会叠加知识库中的语义检索结果来增强准确性。

对于每篇候选文献，系统调用一个轻量AI模型（通过Phase 1.2的路由器）生成"推荐理由"（一句话说明为什么这篇论文适合在这里引用）。

引用格式的渲染使用CSL（Citation Style Language）标准。系统内置常用的几种格式（APA、IEEE、GB/T 7714中文国标），用户可以自行导入更多.csl文件。渲染结果以纯文本形式插入到写作应用中，或者写入剪贴板。

将引用插入到Word的过程需要系统级集成。在Windows上，可以通过Word的COM接口操作文档，精确替换选中文字。如果COM接口不可用，回退到模拟键盘输入。在macOS上，可以通过AppleScript让系统模拟键盘操作。如果这两种方式都失败，系统回退到最可靠的方式：把格式化的引用文本写入剪贴板，然后模拟一次Ctrl+V粘贴操作，并给用户一个短暂的气泡提示"已粘贴"。

**Obsidian集成技术路径**：Obsidian本质上是一个基于本地Markdown文件的笔记系统。系统与它交互的主要方式是直接文件系统操作：在Obsidian Vault的指定文件夹下创建.md文件。Obsidian会实时检测到新文件并自动索引。

为了让保存的笔记符合Obsidian生态的惯例，系统生成的Markdown包含标准的YAML frontmatter（元数据头），里面记录标题、日期、标签、来源、所属项目等信息。标签自动从对话内容中提取高频术语生成。笔记正文采用结构化模板，用户可以在设置中选择不同模板（摘要版、完整版、Q&A版），也可以自己在Obsidian的模板文件夹里创建自定义模板。

双向链接的建立需要系统在保存笔记前，先扫描用户Vault中已有的笔记标题，找出与当前对话内容相关的已有笔记。检测方式有两种：一是精确匹配（对话中出现了某个已有笔记的标题），二是语义相似（对话主题与某篇笔记标题的向量相似度超过阈值，利用Phase 1.3的知识库向量化能力）。匹配上的笔记名会被转换为Obsidian的Wikilink格式（[[笔记标题]]）。

系统也支持通过Obsidian的URI协议（obsidian://open?vault=...&file=...）在外部直接打开特定笔记，这样用户保存后可以直接跳转到Obsidian查看。

### 与现有系统的衔接

- **与Alt+Q快捷菜单的衔接**：快捷菜单的内容现在是动态变化的——系统检测当前活动窗口类型（依赖Phase 0的窗口检测），如果是Word/Overleaf/Typora/VS Code等写作应用，菜单里就显示"推荐引用"；如果是浏览器或PDF阅读器，则不显示。这个检测通过定时轮询活动窗口进程名实现（每3秒检查一次，开销极小）。
- **与主窗口聊天系统的衔接**：每条AI回复消息右上角的操作菜单是新增的，不影响原有的消息展示和流式输出逻辑。点击"存到Obsidian"时，如果当前对话属于某个项目（Phase 1.1），则笔记默认保存到该项目关联的Vault子目录下。
- **与项目系统的衔接**：每个项目可以独立配置关联的Zotero收藏夹和Obsidian Vault路径。项目切换时，引用推荐和笔记保存的目标自动切换。
- **与知识库的衔接**：Zotero文献的元数据被同步到本地缓存表后，如果用户同时开启了知识库功能，这些文献的全文内容也可以被知识库索引。这样用户在引用推荐时既能命中Zotero的元数据，也能利用知识库的语义检索能力。
- **与Phase 0事件采集的衔接**：每次Zotero同步完成记录zotero_sync事件。每次Obsidian笔记保存记录note_save_to_obsidian事件。每次引用插入记录citation_insert事件。

---

## Phase 2 完成状态（验收标准）

达到以下状态，Phase 2才算完成：

1. **Zotero联动**：系统能与Zotero本地API通信，维护增量同步的本地缓存。用户在写作应用中选中文字后，可以通过Alt+Q获取基于自己Zotero库的智能引用推荐。引用可以以多种格式（APA/IEEE/GB7714）插入到写作应用中。
2. **Obsidian联动**：用户可以将AI对话保存为Obsidian笔记，系统自动生成YAML frontmatter和双向链接。笔记保存后可以通过URI协议在Obsidian中打开。
3. **数据层面**：zotero_items_cache和zotero_collections_cache表正常工作，增量同步逻辑稳定。
4. **用户感知**：用户可以在写作时按Alt+Q获取引用推荐（来自自己Zotero库的真实论文），可以把AI对话一键保存到Obsidian（带双向链接）。产品正式连接了科研生态中的两个核心工具。

**Phase 2 → Phase 3 的衔接说明**：进入Phase 3后，写作伴侣将利用Phase 2建立的Zotero桥来实现"选中即润色"和"引用推荐"两个核心能力。阅读伴侣将利用Phase 2建立的Obsidian桥来实现"阅读笔记保存"功能。窗口检测框架（Phase 0）将为写作伴侣和阅读伴侣提供"当前用户在用什么应用"的实时信息。

---

# Phase 3：高频工作流（约3周）

## 本Phase目标

实现两个用户日常使用频率最高的功能——写作伴侣和阅读伴侣。这两个功能共享Phase 0建立的窗口检测框架，放在一起开发效率最高。Phase 3完成后，用户在使用Word写论文或用PDF阅读器读文献时，悬浮球会变成真正的"智能伴侣"。

## 前置条件

- Phase 0的窗口检测框架稳定运行
- Phase 1的项目系统、多模型路由、知识库全部上线
- Phase 2的Zotero/Obsidian桥已打通

---

## 3.1 功能六：写作伴侣模式

### 为什么先做写作伴侣

写作场景的使用频率通常高于阅读场景。而且写作伴侣的技术难度（窗口检测+选中文字获取+文本回写）与阅读伴侣高度重叠，先把这些技术难点都趟平，阅读伴侣的开发就会快很多。写作伴侣做好了，阅读伴侣只需要专注在PDF检测、页码推断、内容类型识别这几个特有逻辑上。

### 功能定义

这是一个在用户撰写学术论文时提供实时AI辅助的模式。系统检测到用户正在写作应用（Word、Overleaf、Typora、VS Code写LaTeX）中编辑文档时，悬浮球的快捷菜单自动切换为"写作专属菜单"，提供三类服务：
- **选中即润色**：用户选中文字后按Alt+Q直接显示润色/改写/翻译选项，结果直接替换原文
- **智能引用推荐**：基于当前段落内容从Zotero库（Phase 2已打通）推荐可引用的文献
- **格式自动修正**：检测并修正不规范的引用格式

所有操作都遵循"不离开写作应用"的原则。

### 界面位置

**第一处：Alt+Q快捷菜单的动态内容。** 这是最核心的界面。当系统检测到用户当前活动窗口是写作应用时（依赖Phase 0的窗口检测），Alt+Q弹出的快捷菜单不再是翻译/净化/格式化，而是替换为写作专属菜单："润色选中文字""简化表达""修正语法""推荐引用""格式检查"。菜单最下方保留原有的"打开设置"和"退出应用"入口。菜单标题显示当前检测到的应用名（"Word写作伴侣"或"Overleaf写作伴侣"）。

**第二处：结果窗口（ResultWindow）的润色对比视图。** 当用户选择润色功能后，结果窗口展示的不是纯文本，而是一个对比视图：左侧是原文，右侧是润色后的文本，修改的部分以高亮（删除红色、新增绿色）标注。下方有三个按钮："采用此版本""查看其他版本""放弃"。

**第三处：引用推荐的独立面板。** 当用户选择"推荐引用"时，弹出一个竖向的引用推荐面板（建议宽度300px，高度自适应），显示3-5篇候选论文。每篇论文是一个卡片，包含：论文标题（可点击展开摘要）、推荐理由（一句话）、引用格式预览（如"[1] Vaswani et al., 2017"）、复选框。用户勾选想要的论文后，底部显示"插入选中的N篇引用"按钮。

**第四处：主窗口增加"写作历史"标签。** 侧边栏除了"会话""文献""待办"，增加"写作"标签，记录用户通过写作伴侣进行的所有润色和引用操作历史，方便回溯和撤销。

### 交互流程

**场景A：选中即润色**

赵老师在Word中写了一段："This method works better than the old one because it can handle more types of data." 她觉得表达太口语化。她选中整句话，按Alt+Q → 快捷菜单弹出，自动显示写作菜单（因为检测到Word是活动窗口）→ 点击"润色为学术英语" → 系统执行：

1. 读取选中的文字（通过辅助功能API或剪贴板）
2. 调用AI模型（通过Phase 1.2的路由器自动选择适合短文本润色的轻量模型），附带System Prompt："请将以下文本润色为正式的学术英语，保持原意，使用被动语态和精确术语。只返回修改后的文本，不要解释。"
3. 获取润色结果："Compared with the previous approach, the proposed method demonstrates superior performance in handling heterogeneous data types."
4. 结果窗口弹出对比视图，原文和润色文并排显示，修改部分高亮
5. 赵老师满意，点击"采用此版本"
6. 系统尝试将润色后的文本直接替换Word中的原文。如果系统级集成成功，原文瞬间被替换；如果不成功，润色文本写入剪贴板，系统模拟Ctrl+V粘贴，并在悬浮球处显示一个短暂的气泡"已替换"

整个过程从选中到替换，耗时约3-5秒。

**场景B：智能引用推荐**

同上场景，赵老师写到："Recently, contrastive learning has emerged as a powerful paradigm for self-supervised representation learning." 她觉得这里应该引用几篇开创性论文。她选中这句话，按Alt+Q → 点击"推荐引用" → 系统执行：

1. 读取选中的文字和前后各3句作为上下文
2. 从这句话中提取关键词："contrastive learning", "self-supervised", "representation learning"
3. 在本地Zotero缓存（Phase 2已建立）和知识库（Phase 1.3已建立）中搜索标题/摘要/关键词与这些词匹配的文献
4. 对搜索结果按综合相关性排序
5. 调用轻量AI模型（通过路由器）为每篇候选文献生成推荐理由
6. 引用推荐面板弹出，显示候选文献卡片

赵老师勾选候选1和候选2，点击"插入" → 系统按当前设定的引用格式（比如IEEE数字上标）生成"[1,2]"并插入到光标位置。

**场景C：引用格式修正**

赵老师从某篇中文综述里复制了一段引用到论文里，格式是："[张三等，2023]"，但她投稿的期刊要求APA格式。她选中这段引用，按Alt+Q → 点击"修正引用格式" → 系统识别出这是一个不规范的引用，解析出作者和年份 → 在Zotero库（Phase 2）中查找匹配条目 → 如果找到，按APA格式重新渲染为"(Zhang et al., 2023)" → 直接替换原文。如果没找到对应条目，系统提示"未在Zotero库中找到匹配文献，请先导入"。

### 实现方案

**写作应用检测**：基于Phase 0的窗口检测框架，实现对Word、Overleaf、Typora、VS Code等写作应用的识别。支持的写作应用包括：Microsoft Word（进程名winword）、WPS文字（wps）、LibreOffice（soffice）、Typora（typora）、VS Code（code，需要额外检测当前编辑的文件扩展名是否是.tex）、TeXstudio（texstudio）、以及通用的浏览器文本输入框（作为Fallback）。

检测逻辑是：先查进程名，如果是浏览器则进一步读取当前标签页的URL或标题来判断是否是在线编辑器（如Overleaf）。检测频率保持每3秒一次。

**选中文字获取**：获取用户在写作应用中选中文字的方式有两种。首选方式是直接通过辅助功能API（Accessibility API）读取当前焦点元素的选中文本，这种方式不经过剪贴板，不会破坏用户原有的剪贴板内容。如果辅助功能API不可用（某些应用不支持），则回退到"复制到剪贴板"方式：系统模拟Ctrl+C，从剪贴板读取内容，然后恢复剪贴板的原始内容（需要提前备份）。

**文本回写（替换原文）**：这是写作伴侣最复杂的技术环节。理想情况下，系统应该直接把处理后的文本写回写作应用的光标位置，替换原有的选中文字。实现方式因平台而异：

- Windows + Word：可以通过Word的COM接口操作文档，精确替换选中文字。如果COM接口不可用，回退到模拟键盘输入。
- macOS + Word：可以通过AppleScript操作Word文档。
- Overleaf/浏览器：浏览器安全沙箱不允许外部程序直接操作网页内容。因此Overleaf场景只能使用"剪贴板+模拟粘贴"的方式。用户选中文字后，系统把处理结果写入剪贴板，然后模拟Ctrl+V。虽然这不是最完美的体验，但Overleaf本身不支持插件生态，这已经是能做到的最好方式。
- Typora/VS Code：这些Electron应用通常支持较好的辅助功能API，可以相对可靠地获取和替换选中文本。

无论哪种方式，如果自动回写失败，系统都会将处理结果写入剪贴板，并弹出一个非侵入式的气泡提示"结果已复制，请粘贴"，确保用户不会丢失结果。

**润色服务**：润色功能本质上是一次AI文本生成任务。系统把用户选中的文字和用户选择的润色风格（学术化、简化、语法修正）组装成一个明确的指令发给AI模型（通过Phase 1.2的路由器自动选择适合短文本的轻量模型）。

为了提供更好的用户体验，润色结果以"对比视图"形式展示，而不是直接替换。这需要前端实现一个文本差异算法，把原文和润色文逐字比对，标出删除和新增的部分。

**引用推荐**：引用推荐的后端逻辑与Phase 2（Zotero联动桥）的智能引用是同一套系统。区别在于触发场景不同——Phase 2是在快捷菜单中手动触发"推荐引用"，写作伴侣模式下是系统主动检测到用户在写论文时自动在菜单中显示该选项。

### 与现有系统的衔接

- **与Alt+Q快捷菜单的衔接**：快捷菜单从静态菜单变为动态菜单。PopupWindow.vue中增加一个computed属性，根据后端传来的当前写作上下文（依赖Phase 0窗口检测）来决定渲染哪些菜单项。原生功能始终在最上方，写作专属菜单项在下方，中间用分隔线隔开。
- **与多模型路由的衔接**：润色任务被路由器自动标记为"短文本改写"类型，优先使用速度快、成本低的模型。引用推荐中的"推荐理由生成"也被标记为"轻量生成"类型。
- **与Zotero联动桥的衔接**：引用推荐和引用格式修正完全复用Phase 2的Zotero HTTP API客户端、本地缓存、CSL渲染引擎。
- **与结果窗口的衔接**：润色结果使用现有的ResultWindow来展示，但展示内容从纯文本改为"对比视图"。ResultWindow中增加一个新的渲染模式（mode="diff"），支持左右分栏+差异高亮。
- **与Phase 0事件采集的衔接**：每次润色操作记录writing_polish事件。每次引用插入记录citation_insert事件。每次格式修正记录writing_format_fix事件。

---

## 3.2 功能五：智能阅读伴侣

### 为什么紧接写作伴侣

阅读伴侣与写作伴侣共享Phase 0建立的窗口检测框架。写作伴侣完成后，阅读伴侣只需要专注在PDF检测、页码推断、内容类型识别、提取触发这几个特有逻辑上，技术难点已经被趟平。

### 功能定义

这是一个在用户阅读PDF论文时主动提供帮助的"伴随式"功能。系统通过检测用户当前正在使用哪个PDF阅读器、在读哪篇论文、读到哪一页，然后适时地提供上下文相关的快捷服务。它不会打扰用户阅读，只在检测到页面上有"值得提取的内容"（比如复杂公式、数据表格、关键定理）时，才在悬浮球上发出 subtle 的视觉提示。用户可以主动点击获取服务，也可以忽略继续阅读。

### 界面位置

**第一处：悬浮球的动态状态指示。** 当系统检测到用户正在读PDF时（依赖Phase 0窗口检测），悬浮球的视觉状态发生微妙变化：球体颜色从默认色变为该文献关联项目的主题色（依赖Phase 1.1项目系统），球体边缘出现一个极细的呼吸灯环。当检测到当前页有可提取内容时，呼吸灯环从柔和变为明显（但仍不刺眼）。用户悬停悬浮球后，展开面板顶部显示当前正在阅读的文献标题（缩短版）和当前页码。

**第二处：悬浮球展开面板的阅读工具菜单。** 当用户悬停悬浮球且系统检测到正在阅读PDF时，展开面板的内容从默认的快捷菜单变为"阅读伴侣"面板，显示："提取当前页公式""提取当前页表格""生成阅读笔记""查看已提取内容"四个按钮。这些按钮旁边有对应快捷键提示（比如F1/F2/F3）。

**第三处：截图窗口（CaptureWindow）的增强模式。** 现有的Alt+S是通用截图提取，现在增加一个"阅读模式"入口。用户在PDF阅读器中按Alt+S时，系统识别出当前在读PDF，截图遮罩层打开的同时，右下角出现一个浮动工具条，提示"阅读模式：拖拽选择区域，系统将自动识别内容类型"。

**第四处：结果窗口（ResultWindow）增加"保存到阅读笔记"选项。** 当用户通过阅读伴侣提取了一个公式后，结果窗口除了"复制"和"关闭"，增加"加入阅读笔记"按钮。点击后该公式被附加到用户正在创建的阅读笔记中。

### 交互流程

**场景A：公式提取**

刘同学用SumatraPDF阅读一篇ICML论文，翻到第5页，看到一个复杂的多行公式。她不需要做任何操作——悬浮球的呼吸灯环从柔和变为明显，提示"这一页有可提取内容"。她可以选择忽略继续阅读，也可以把鼠标悬停到悬浮球上 → 展开面板显示"检测到公式，按F2提取" → 她按F2 → 系统执行以下动作：

1. 获取当前PDF文件路径和页码（依赖Phase 0窗口检测）
2. 如果该PDF已经在知识库中（Phase 1.3），直接读取该页的文本内容；如果不在，临时提取该页文本
3. 使用专门针对数学公式的模式识别，从文本中定位公式区域
4. 将公式转换为LaTeX代码
5. 结果窗口弹出，显示LaTeX代码和预览
6. 同时，系统自动记录"《论文标题》- 第5页 - 公式已提取"

刘同学点击"复制"获取LaTeX代码，继续阅读。整个过程不需要离开PDF阅读器。

**场景B：阅读笔记生成**

刘同学读完第3-8页后，想记录一下这页的核心内容。她悬停悬浮球 → 点击"生成阅读笔记" → 系统分析这几页的文本内容，生成一个结构化的阅读笔记模板，包含：

- 文献基本信息（自动填入：标题、作者、期刊/会议、年份——来自Zotero元数据，Phase 2）
- 阅读日期和页码范围
- 核心观点（AI自动总结的2-3句话——通过Phase 1.2路由器调用轻量模型）
- 方法概述
- 关键发现
- 局限性（从论文的Limitations段落提取）
- 与自己研究的关系（留空，由用户自己填写）
- 已提取的公式/表格列表（自动列出刚才提取的所有内容）

笔记以Markdown格式生成，用户可以选择保存到Obsidian Vault（如果配置了Obsidian连接，Phase 2）或者保存为本地Markdown文件。

**场景C：连续阅读的进度追踪**

刘同学连续阅读了45分钟，中间翻看了12页。系统后台默默记录着阅读时长和浏览过的页码。当她关闭PDF阅读器时，知识库中该文献的条目自动更新阅读统计（"累计阅读45分钟，已读12/20页"）。下次她打开同一篇PDF时，系统通过悬浮球提示"上次读到第8页，继续？"。

### 实现方案

**PDF阅读器检测**：基于Phase 0的窗口检测框架，专门识别PDF阅读器进程和PDF文件名提取。支持SumatraPDF、Adobe Acrobat、Foxit Reader、Zotero内置阅读器、以及浏览器中的PDF预览。对于Zotero内置阅读器，窗口标题通常包含文献标题，系统通过Zotero API（Phase 2）反向查询到对应的PDF文件路径。

对于无法从窗口标题推断文件路径的情况，系统使用平台特定的技术来定位：Windows上可以通过查询进程的打开文件句柄来反推文件路径；macOS上可以通过Accessibility API读取窗口的AXDocument属性。

**当前页码估计**：很多PDF阅读器在窗口标题中包含页码信息（如"Paper Title - Page 5 of 20"），系统用正则表达式提取。如果标题中没有页码，系统可以通过Accessibility API（如果阅读器支持）或截图后OCR识别页面角落的页码数字来估计。

**内容类型检测**：获取到当前页的文本后，系统用一组启发式规则判断页面上有什么值得提取的内容类型。公式检测规则包括：文本中包含大量LaTeX标记（$、$$、\begin{equation}、\frac、\sum等），且公式标记密度超过阈值。表格检测规则包括：多行文本呈现规律的对齐结构（比如很多空格分隔的列，或者包含|分隔符）。定理检测规则包括：段落开头出现"Definition"、"Theorem"、"Lemma"等关键词。

当检测到某种内容类型时，系统更新悬浮球的视觉状态。但为了避免频繁闪烁打扰用户，系统设置了"冷却期"——同一页上同类型的内容30秒内只提示一次。

**公式/表格提取**：对于公式，系统优先从PDF的文本层直接提取（因为PDF内部通常就包含LaTeX源或Unicode数学符号），这比截图后OCR更准确。只有当PDF是扫描版（没有文本层）时，才回退到截图+视觉模型OCR（通过Phase 1.2路由器调用视觉模型）。对于表格，系统尝试从PDF文本层的布局信息重建Markdown表格结构，失败时回退到OCR。

**阅读进度追踪**：阅读进度以会话形式存储。当系统检测到用户开始阅读某篇PDF（该PDF窗口成为活动窗口超过10秒），就创建一个新的ReadingSession记录。会话持续期间，系统记录用户浏览过的页码集合（通过定时轮询当前页码）。当PDF窗口不再是活动窗口超过5分钟，或者用户关闭了PDF阅读器，会话结束，累计时长和浏览页数被保存。这些数据后续用于知识库中的"阅读统计"展示和AI的"上次读到哪"提示。

### 与现有系统的衔接

- **与悬浮球的衔接**：悬浮球是阅读伴侣功能的核心交互载体。现有的悬浮球只有"单击打开快捷菜单、双击打开主窗口"两种操作。现在增加第三种状态感知："悬停时根据当前场景显示不同面板"。这需要扩展PopupWindow的内容渲染逻辑，使其能根据系统检测到的用户当前活动（在读PDF/在写作/在浏览网页/在写代码）动态切换面板内容。
- **与知识库的衔接**：阅读伴侣提取的内容自动关联到知识库中的对应文献条目。如果这篇PDF还没有被索引到知识库（Phase 1.3），系统会自动触发一次快速索引（只索引当前页附近的几页，而不是全文，以加快速度）。
- **与Alt+S截图提取的衔接**：阅读伴侣的公式/表格提取和Alt+S的通用截图提取共享同一个后端视觉提取引擎，但阅读伴侣走的是"文本层优先"路径（更准确、更快），Alt+S走的是"视觉模型通用OCR"路径（适用性更广）。两条路径的结果都以相同的格式输出到结果窗口。
- **与项目系统的衔接**：阅读进度和阅读笔记都归属于当前活跃的项目（Phase 1.1）。用户在项目A下读论文，提取的笔记默认保存到项目A的笔记目录。
- **与Zotero/Obsidian桥的衔接**：阅读笔记的文献基本信息从Zotero元数据获取（Phase 2），笔记保存到Obsidian Vault（Phase 2）。
- **与Phase 0事件采集的衔接**：PDF打开/关闭/翻页时记录pdf_open/close/page_change事件。公式/表格提取时记录capture_ocr_complete事件。阅读笔记保存时记录note_create事件。

---

## Phase 3 完成状态（验收标准）

达到以下状态，Phase 3才算完成：

1. **写作伴侣**：在Word/Overleaf/Typora/VS Code中选中文字后按Alt+Q，快捷菜单自动切换为写作菜单。润色功能展示diff对比视图，结果可直接替换原文或复制粘贴。引用推荐基于用户自己的Zotero库，支持多种引用格式插入。
2. **阅读伴侣**：当用户在PDF阅读器中阅读时，悬浮球自动切换为阅读伴侣状态（呼吸灯、展开面板显示阅读工具）。系统能检测到公式/表格/定理并 subtle 提示。按F2等快捷键可直接提取当前页内容。阅读进度自动追踪。
3. **用户感知**：这是用户感知最强的Phase。用户会发现"我在Word里写论文时Alt+Q变成了写作助手""读PDF时悬浮球会提醒我提取公式"。悬浮球已经从"快捷入口"进化为"智能伴侣"。

**Phase 3 → Phase 4 的衔接说明**：进入Phase 4后，文献哨兵将利用Phase 1.1的项目系统和Phase 1.3的知识库来推断研究方向和检索相关文献。实验记录将利用Phase 1.1的项目系统来归属记录。综述自动化将综合利用Phase 1的全部三个功能（项目上下文+知识库全文+多模型路由）以及Phase 2的Zotero桥来获取文献列表。

---


# Phase 4：自动化与进阶功能（约3-4周）

## 本Phase目标

实现三个相对独立但价值很高的自动化功能。它们都依赖前面Phase的基础设施，但彼此之间没有强依赖，可以按顺序或并行开发。Phase 4完成后，产品覆盖了科研工作的更多场景——从被动阅读（每周自动推送新文献）到主动记录（实验快照）再到批量产出（综述草稿）。

## 前置条件

- Phase 0-3全部完成
- 项目系统、知识库、多模型路由、Zotero/Obsidian桥、写作伴侣、阅读伴侣均已上线

---

## 4.1 功能二：智能文献哨兵

### 为什么先做这个

文献哨兵的实现相对独立，不依赖写作伴侣或阅读伴侣。且它能给用户带来"被动价值"——即使不主动操作，每周也能收到有价值的新文献推送。这提升了产品的用户粘性。先做它，可以让用户在Phase 4的早期就感受到价值，而不是等到Phase 4末尾。

### 功能定义

这是一个"替你盯梢"的自动化功能。系统通过分析用户近期的研究行为（聊天话题、本地文献库的主题分布），自动推断出用户当前在关注什么研究方向，然后定期去arXiv、Semantic Scholar等学术数据库里查找刚发表的相关论文，把匹配结果整理成一份"本周新文献简报"推送给用户。用户不需要手动设置任何关键词，也不需要订阅任何邮件列表。

### 界面位置

**第一处：悬浮球的被动提醒。** 当系统检测到新的匹配论文时，悬浮球边缘会出现一个 subtle 的小红点（类似微信未读消息）。用户把鼠标悬停到悬浮球上时，展开的面板顶部会出现"本周有N篇新文献"的横幅，点击后打开简报窗口。

**第二处：独立的"文献简报"窗口（新窗口类型，尺寸建议360x480，alwaysOnTop）。** 这个窗口每周一早晨自动弹出一次（如果用户开启了自动推送），或者在用户点击悬浮球的红点后手动弹出。窗口内按研究方向分组展示新论文，每组有一个AI生成的趋势洞察（比如"本周出现了两篇关于将对比学习用于3D医学影像的论文，说明这个交叉方向正在升温"）。

**第三处：主窗口侧边栏的"哨兵"标签页。** 与"会话""知识库""待办"并列（都是Phase 1.1建立的标签体系），展示用户当前设置的所有监控主题（每个项目可以有多个主题），以及每个主题最近查到的新论文数量。用户可以在这里手动创建监控主题、暂停某个主题的监控、或者一键将所有新论文导入知识库。

### 交互流程

**自动运行路径（无用户操作）：**

系统后台每6小时执行一次检查（通过tokio::time::interval定时任务）。假设李教授正在研究"多模态大模型的幻觉问题"——这个方向是系统自动推断出来的（见实现方案）。周一早上9点，系统完成一轮检查后发现有7篇相关新论文。此时悬浮球出现红点。李教授看到红点，悬停悬浮球，看到提示"发现7篇新论文"，点击进入简报窗口。窗口中显示：

- 主题"多模态大模型幻觉"：5篇新论文
  - 论文1：标题 + AI生成的单句摘要 + "来自你关注的作者王XX"标签
  - 论文2：...（以下省略）
- 主题"对比学习"（次要方向）：2篇新论文

李教授对论文1感兴趣，点击"查看原文"用浏览器打开arXiv页面；点击"一键入库"则系统自动下载PDF并加入知识库索引（Phase 1.3）；点击"忽略"则这篇论文以后不再推送。

**手动管理路径：**

用户打开主窗口 → 切换到"哨兵"标签 → 看到系统自动推断出的监控主题列表 → 觉得某个方向已经不需要关注了，点击右侧的暂停按钮；或者点击"+ 添加监控主题"手动输入关键词（比如"NeRF城市重建"）。每个主题可以独立设置监控数据源（arXiv、Semantic Scholar、Google Scholar、PubMed）和检查频率（每天/每三天/每周）。

**方向推断校准路径：**

如果用户发现系统推断的研究方向不准（比如一直在推"计算机视觉"但用户已经转做"自然语言处理"了），可以打开哨兵设置，点击"重新推断研究方向"，系统会重新分析最近30天的聊天记录和本地文献库关键词分布，生成新的主题建议供用户确认。

### 实现方案

**研究方向推断**：系统需要一个"主题推断引擎"，定期（比如每周一次）分析两类信号：

- 信号一：用户在各个项目中的AI对话记录（从chat_messages表读取最近50条用户发送的消息），提取用户最近频繁提到的技术术语和论文名称。
- 信号二：本地知识库中最近新增文献的关键词分布（从knowledge_docs表的metadata字段提取关键词）。

Phase 1.3的知识库建立后，系统就有了文献关键词数据。把这个信号和对话信号汇总后，调用一个轻量级的本地模型（通过Ollama运行，2B参数级别）来提取3-5个核心研究方向。每个方向用一组关键词表示，比如"multimodal hallucination, vision-language model, factual consistency"。

推断结果默认自动生效，但会通知用户"系统认为你在关注这些方向，是否正确？"用户可以调整或删除。每个监控主题归属于某个项目（Phase 1.1），存储在sentinel_topics表中。

**定期检索调度**：系统维护一个轻量的后台调度器，使用定时任务每6小时触发一次。为了避免所有用户同时请求arXiv服务器造成压力，每次启动时加入一个随机延迟（0到1小时之间）。调度任务运行时，依次查询用户设置的每个监控主题。

- **arXiv查询**：调用arXiv的免费API（http://export.arxiv.org/api/query），不需要申请Key。使用all:关键词组合作为搜索参数，按submittedDate降序排列，限制最近7天的结果。
- **Semantic Scholar查询**：调用其免费API（https://api.semanticscholar.org/graph/v1/paper/search），有每日请求上限（100次），需要控制频率。
- **Google Scholar**：通过RSS Feed或解析搜索页面获取。稳定性较差，作为备选数据源。

检索结果通过标题+DOI做去重，避免同一篇论文从不同数据源重复出现。新发现的论文存入sentinel_papers表。

**相关性过滤与排序**：检索到的新论文首先经过一层粗筛——标题和摘要必须包含监控主题的关键词。然后进入精筛：将论文的标题+摘要转换成向量（利用Phase 1.3知识库的嵌入模型），与用户的"研究方向向量"（由该主题下所有本地文献的平均向量表示）计算相似度，只保留相似度超过阈值（0.6）的论文。最后按"综合热度"排序——考虑发表时间（越新越靠前）、作者关注度（如果该作者是用户本地文献库中出现过的高频作者则加分）、引用增长速度（Semantic Scholar API可提供）。

**简报生成**：对于每个监控主题，系统把该主题下本周的所有新论文列表传给AI（通过Phase 1.2路由器调用轻量模型），要求AI生成一段趋势洞察文字（不超过3句话）。这段文字会出现在简报窗口的该主题头部，帮助用户快速把握这个方向的动态。

### 与现有系统的衔接

- **与项目系统的衔接**：每个监控主题必须归属于某个项目（Phase 1.1）。这样当用户在项目A下工作时，只会收到项目A相关方向的新论文推送。项目切换时，哨兵监控的主题集合自动切换。
- **与知识库的衔接**：简报中的每篇论文都有"一键入库"按钮。点击后，系统自动下载PDF（从arXiv可以直接推断PDF链接），保存到该项目关联的文献文件夹，并触发Phase 1.3知识库的自动索引流程。论文入库后，用户就可以在聊天中通过知识库检索引用这篇新论文了。
- **与多模型路由的衔接**：趋势洞察的生成调用轻量模型（通过路由器自动选择），不需要长上下文模型。如果本地Ollama可用，优先使用本地模型（零API费用）。
- **与悬浮球的衔接**：悬浮球是用户看到提醒的第一触点。红点提醒需要控制频率——如果用户连续3次都没有点击红点查看，系统会降低该主题的检查频率，并把红点改为更 subtle 的色块提示。
- **与Phase 0事件采集的衔接**：每次哨兵检查完成记录sentinel_check事件（带上发现的论文数和用户阅读的论文数）。

---

## 4.2 功能七：实验记录快照

### 为什么在这个位置做

实验记录依赖项目系统（Phase 1.1，保存到项目目录）和事件采集系统（Phase 0），但不依赖R3的Zotero桥或知识库。它可以与文献哨兵并行开发。先做文献哨兵再做实验记录，是因为文献哨兵给用户带来的"被动价值"更直接（每周自动推送），而实验记录是"主动操作"功能，使用频率取决于用户是否做实验。

### 功能定义

这是一个专门为理工科实验型研究者设计的快速记录工具。用户在做实验的过程中（比如跑深度学习训练、调试代码、操作实验仪器），可以随时按一个快捷键唤出一个极简的记录界面，系统自动抓取当前屏幕的关键信息（终端的输出日志、代码编辑器里的参数设置、实验仪器的读数界面），生成一条带精确时间戳的结构化实验记录。记录可以包含截图、文字描述、键值对形式的实验参数、以及一段语音备注（系统自动将语音转为文字）。所有记录按时间线组织，支持按项目和关键词检索，让实验过程完全可追溯。

### 界面位置

**第一处：新增全局快捷键Alt+E（Experiment）。** 这个快捷键与现有的Alt+Q（快捷菜单）和Alt+S（截图提取）并列，构成三大核心入口。按Alt+E后，屏幕中央偏下位置弹出一个小型面板（建议尺寸320x200，alwaysOnTop），标题为"实验快照"。

**第二处：实验快照面板本身。** 面板非常简洁，顶部显示当前时间，中间是四个记录类型的大图标按钮（横排）："截屏""终端""代码""语音"。点击某个按钮后，面板内容切换为该类型的记录界面。面板底部是"参数"区域，允许用户快速输入键值对（比如lr=0.001, batch=64），和"备注"文本框。

**第三处：主窗口侧边栏新增"实验"标签页。** 与"会话""知识库""待办"并列（Phase 1.1建立的标签体系），显示按时间倒序排列的所有实验记录卡片。每张卡片显示时间、标题、类型图标、关联项目名。点击卡片展开详情，显示截图预览、参数表格、语音转录文字。

**第四处：悬浮球展开面板的快捷入口。** 当用户把鼠标悬停在悬浮球上时，展开面板的快捷菜单中增加"实验快照"图标（在原有菜单项旁边增加，不替换原有项）。

### 交互流程

**场景A：深度学习训练记录**

周同学在终端里启动了深度学习训练，屏幕上显示着TensorBoard的实时loss曲线和终端的epoch输出。训练跑到第10个epoch时，他想记录一下当前状态。他按Alt+E → 屏幕中央弹出实验快照面板 → 默认选中"截屏"类型 → 他看到面板中间实时预览了当前全屏的小缩略图 → 点击"截取终端区域" → 面板切换为区域选择模式，类似Alt+S的截图遮罩但简化版（只有"全屏""当前窗口""手动选区"三个按钮）→ 他选择"当前窗口" → 系统自动识别终端窗口的位置和大小，截取该区域 → 截图显示在面板中央。

然后他在底部的"参数"区域快速输入：
- lr: 0.001
- batch_size: 64
- optimizer: AdamW
- scheduler: cosine

在"备注"框里打字："第10个epoch，loss还在下降"

点击"保存" → 面板关闭，右下角弹出"实验快照已保存"的气泡 → 这条记录被存入数据库（experiment_snapshots表），文件保存到项目关联的实验记录文件夹中（以Markdown格式），同时主窗口侧边栏"实验"标签的列表顶部新增一条记录。

**场景B：语音备注**

周同学不想打字，他想口述一些观察。在实验快照面板中，他点击"语音"图标 → 面板显示一个大的录音按钮，文字提示"按住空格录音，松手结束" → 他按住空格，对着麦克风说"这次调整了学习率从0.01降到0.001，观察收敛速度有没有提升" → 松手 → 系统开始语音转录（调用本地Whisper模型或在线API）→ 3秒后转录文字显示在备注框中 → 他检查无误后点击保存。

**场景C：实验记录的检索与回顾**

一周后，周同学需要对比不同学习率设置的实验效果。他打开主窗口 → 切换到"实验"标签 → 在顶部搜索框输入"学习率" → 系统搜索所有实验记录的标题、参数、备注和语音转录文字 → 返回4条匹配记录，按时间倒序排列。每条记录展开后显示：截图预览（可点击放大）、参数表格、备注文字、当时所属的项目和日期。他对比了lr=0.01和lr=0.001的两条记录，根据截图中的loss曲线做出了决策。

### 实现方案

**记录存储结构**：每条实验记录包含三个层面的数据。

- 元数据层面（存储在SQLite的experiment_snapshots表中）：记录ID、所属项目ID、时间戳、标题、类型、参数键值对（JSON）、标签、来源文件路径。
- 文件层面：截图保存为WebP格式（压缩率高，节省空间），存放在用户的实验记录文件夹下，按年月分子目录（experiments/2026-04/）。语音文件（如果有）保存为WAV。
- 文本层面：系统同时生成一份Markdown文件，包含YAML frontmatter（记录所有元数据）和正文（备注文字、参数表格、截图引用）。这个Markdown文件可以直接被Obsidian识别和渲染。

**截图实现**：复用现有的截图基础设施（screenshots crate）。实验快照的截图比Alt+S的通用截图更简单——不需要复杂的选区逻辑，主要提供三种模式：全屏截图（默认）、当前活动窗口截图（自动识别窗口边界，依赖Phase 0窗口检测）、以及手动选区（简化版的拖拽选区）。截图后自动压缩为WebP格式，质量设置为85%。

**语音采集与转录**：语音功能使用跨平台的音频采集库来录制麦克风输入，保存为WAV格式。录音界面采用"按住说话"的交互（类似微信），降低用户的操作成本。语音转录有两种实现路径：如果用户本地安装了Ollama并加载了Whisper模型，则完全本地转录（隐私最好、不消耗API费用）；如果本地没有，则调用在线的语音识别API（如OpenAI Whisper API）。转录结果实时显示在备注框中，用户可以手动修正。

**终端内容提取**：当用户选择"终端"类型时，系统尝试自动捕获当前终端窗口中的文本内容。这在不同终端模拟器中的实现难度不同。对于Windows的Windows Terminal和macOS的Terminal.app，可以通过辅助功能API读取窗口内的文本。如果无法直接读取，系统回退到截图+OCR的方式——截取终端区域，用OCR识别文本。识别的文本自动填入备注框，同时截图作为附件保存。

**代码参数自动提取**：当用户选择"代码"类型且当前活动窗口是代码编辑器（VS Code、PyCharm等）时，系统尝试获取当前打开的文件路径，读取文件内容，然后使用简单的正则匹配来提取常见的实验参数变量（如lr、learning_rate、batch_size、epochs、optimizer等）。自动提取的参数显示在参数区域，用户可以自由修改或删除。

### 与现有系统的衔接

- **与全局快捷键体系的衔接**：Alt+E是新增加的全局快捷键，需要在Rust后端通过tauri-plugin-global-shortcut注册。Alt+E的响应流程与Alt+Q/S类似：唤起一个独立的小窗口（实验快照面板），处理完成后关闭。
- **与项目系统的衔接**：实验记录必须归属于某个项目（Phase 1.1）。当用户按Alt+E时，系统默认将记录归入当前活跃的项目。如果用户当前没有激活任何项目，弹出提示"请先选择一个项目，或保存到通用记录"。
- **与知识库的衔接**：实验记录的Markdown文件可以被知识库（Phase 1.3）自动索引。由于实验记录中包含大量结构化参数数据，索引后用户可以在知识库中搜索"lr=0.001"这样的精确参数查询。
- **与Obsidian桥的衔接**：由于实验记录本身就是标准Markdown格式，如果用户在项目设置中配置了Obsidian Vault路径（Phase 2），系统直接把Markdown文件保存到Vault的指定子目录下。Obsidian会自动识别并渲染这些笔记。
- **与Phase 0事件采集的衔接**：创建实验记录时记录experiment_snapshot事件。

---

## 4.3 功能八：文献综述自动化

### 为什么放在Phase 4最后

综述自动化依赖的功能最多——需要RAG（Phase 1.3，获取文献全文）、Zotero桥（Phase 2，获取文献列表）、多模型路由（Phase 1.2，调用长上下文模型）、项目系统（Phase 1.1，按项目生成）。只有当这些都完成后，综述自动化才能真正跑通。它是Phase 4的"压轴功能"，综合运用了前面所有基础设施。

### 功能定义

这个功能帮助用户基于自己已经收集和阅读过的文献，一键生成结构化的文献综述草稿。用户指定一组相关论文（从Zotero收藏夹里选择，或者从知识库里手动勾选），系统逐篇分析每篇论文的核心贡献、方法、数据集、评估指标和局限性，然后综合所有分析结果，生成一份包含研究背景、方法分类、对比表格、研究空白识别和未来方向的综述文档。综述以Markdown格式输出，用户可以在此基础上修改完善。

### 界面位置

**第一处：主窗口侧边栏"知识库"标签页顶部新增"生成综述"按钮。** 用户在知识库中看到一堆相关文献后，可以点击这个按钮启动综述生成流程。

**第二处：快捷菜单（Alt+Q）新增"生成综述"选项。** 当系统检测到用户剪贴板里是一段文献列表或者多行DOI时，快捷菜单智能显示"生成综述"入口。

**第三处：独立的"综述生成"向导窗口（新窗口，尺寸600x500，居中显示）。** 这是一个分步向导界面：第一步让用户选择文献来源（从Zotero收藏夹选 / 从知识库选 / 粘贴DOI列表）；第二步显示选中的文献列表，每篇显示标题和作者，用户可以勾选/取消；第三步选择综述风格（"学术综述"适合毕业论文/期刊投稿，"调研简报"适合快速了解领域，"简要概述"适合组会汇报）；第四步确认并开始生成。

**第四处：结果展示窗口（ResultWindow）用于展示生成中的进度和最终综述。** 综述生成是一个耗时的后台任务，用户在向导中点击开始后，ResultWindow打开，显示一个进度条（"正在分析第3/15篇论文..."）和实时生成的内容预览（随着每篇论文分析完成，综述的对应章节逐渐填充）。用户可以在生成过程中取消。

### 交互流程

**场景A：从Zotero收藏夹生成综述**

吴同学需要为毕业论文写第二章文献综述，涉及15篇核心论文。他在Zotero中有一个"毕业论文/文献综述"收藏夹，里面正好存放了这15篇论文。他打开主窗口 → 切换到知识库标签 → 点击"生成综述" → 综述向导窗口打开 → 第一步选择来源：他点击"从Zotero选择"（Phase 2的Zotero桥提供收藏夹数据）→ 弹出Zotero收藏夹树形列表 → 选择"毕业论文/文献综述" → 系统加载该收藏夹里的15篇论文，显示在第二步的列表中 → 他确认15篇都正确，点击下一步 → 第三步选择风格："学术综述"，输入聚焦方向"对比学习在遥感图像中的应用" → 点击"开始生成"。

ResultWindow打开，显示进度：
- Phase 1 "逐篇分析"：进度条走到15/15，每走一格显示当前正在分析的论文标题
- Phase 2 "主题聚类"：系统根据论文关键词的相似度自动把它们分成几个方法类别
- Phase 3 "综合生成"：综述正文逐段生成，实时显示在预览区

约5-10分钟后（取决于模型速度和论文数量），生成完成。最终综述包含：

1. 研究背景与动机（AI综合所有论文的Introduction段落生成）
2. 方法分类（按聚类结果的组别分节，每节介绍一个方法流派）
3. 对比表格（15篇论文 × 6个维度：方法、数据集、评估指标、核心创新、优势、局限性）
4. 研究空白（系统分析所有论文的Limitations部分，找出共性和缺口）
5. 未来方向（基于研究空白推断）
6. 参考文献列表（从Zotero自动导出）

吴同学查看预览，觉得"研究空白"部分的第3点不太准确，他在预览中直接编辑修正。满意后点击"保存到Obsidian"（Phase 2的Obsidian桥），综述以Markdown格式保存到Vault中。他在这个基础上继续修改，最终完成自己的综述章节。

**场景B：从知识库手动选择**

如果用户没有在Zotero里整理收藏夹，也可以在知识库标签页中手动勾选文献——每篇文献卡片旁边增加一个复选框，勾选后点击"用选中的N篇生成综述"即可进入向导的第二步。

### 实现方案

**综述生成的核心逻辑采用"Map-Reduce"范式**，这是处理多文档综合分析的经典策略。

**Map阶段（逐篇分析）**：对于用户指定的每一篇论文，系统首先确保该论文的全文内容已经在知识库中建立了索引（Phase 1.3）。如果还没索引，则先触发快速索引。然后，系统把这篇论文的文本内容（通常截断到前12000字，覆盖摘要+引言+方法+实验部分）发送给AI模型（通过Phase 1.2路由器调用支持长上下文的模型），附带一个结构化的提取指令："请分析以下论文，提取其核心贡献、方法名称、使用的数据集、评估指标、主要结果、作者指出的局限性，以及2-4个关键词。以JSON格式返回。"

每篇论文的分析是独立的，因此可以并行处理（同时向AI发起多个请求，只要API提供商的并发限制允许）。15篇论文可以同时处理，大大缩短总时间。每篇分析结果包含结构化的字段，后续步骤可以直接使用。

**主题聚类阶段**：拿到15篇论文的结构化分析结果后，系统需要把它们分组。分组的依据是论文的关键词和方法描述。系统把每篇论文的关键词拼接成一个短文本，转换成嵌入向量（复用Phase 1.3知识库的向量化基础设施），然后对这些向量做聚类——把相似的论文自动归为一组。比如所有提到"InfoNCE loss"和"instance discrimination"的论文会被分到同一组。聚类结果自动命名（用该组论文中出现频率最高的关键词组合作为组名）。

**Reduce阶段（综合生成）**：聚类完成后，系统进入综合写作阶段。它依次生成综述的各个章节：

- 研究背景：把所有论文的"核心贡献"汇总，让AI判断这个领域的研究动机和演进脉络。
- 方法分类：按聚类结果的组别，每一组写一个小节，介绍这个流派的核心思想、代表性工作和与其他流派的关系。
- 对比表格：直接从Map阶段提取的结构化数据中组装表格。这是纯数据处理，不需要AI生成，确保准确性。
- 研究空白：把所有论文的"局限性"字段汇总，让AI分析这些局限性的共性模式，找出当前领域尚未解决的关键问题。
- 未来方向：基于研究空白，让AI推断未来3-5年可能的研究热点。

每个章节的生成都调用一次AI（通过路由器选择适合长文本生成的模型），但输入的上下文是经过精心裁剪的摘要信息（而不是全文），确保不超出模型的上下文长度限制。

**用户交互与进度反馈**：由于整个综述生成可能需要5-15分钟，系统通过ResultWindow向用户展示实时进度。Map阶段的进度很容易量化（15篇中的第N篇），系统逐格推进进度条，并在每个格子旁边显示当前论文的短标题。进入Reduce阶段后，进度条切换为章节模式（5个章节逐一点亮），每个章节生成完成后立即显示在预览区，用户可以先读先生成的部分，不必等全部完成。

**结果保存与编辑**：生成的综述以Markdown格式存储。ResultWindow的预览区本身就是一个可编辑的文本区域（或基于Markdown的轻量编辑器），用户可以直接在预览区做修改。点击"保存"后，系统提供两个保存目标：保存为本地Markdown文件，或者推送到Obsidian Vault（Phase 2的Obsidian桥）。

### 与现有系统的衔接

- **与知识库的衔接**：综述生成完全依赖知识库中已索引的文献全文。如果用户选择的某篇论文还没有被索引，系统会提示"该论文尚未索引，是否立即索引？"索引完成后才继续。
- **与Zotero联动桥的衔接**：综述向导的第一步直接复用Phase 2的Zotero API客户端获取收藏夹结构和文献列表。参考文献列表的导出也复用Zotero桥的缓存数据。
- **与项目系统的衔接**：综述任务归属于当前活跃的项目（Phase 1.1）。生成的综述自动保存在该项目的目录下，综述中引用的所有论文自动关联到项目文献库。
- **与多模型路由的衔接**：综述生成涉及大量长文本处理（每篇论文上万字），多模型路由会自动选择支持长上下文的模型（比如128K上下文窗口的模型）来执行逐篇分析。而章节综合写作则使用擅长长文写作的模型。
- **与Phase 0事件采集的衔接**：综述生成开始和完成时分别记录synthesis_start和synthesis_complete事件。

---

## Phase 4 完成状态（验收标准）

达到以下状态，Phase 4才算完成：

1. **文献哨兵**：系统能自动推断研究方向，每6小时检查一次arXiv/Semantic Scholar。每周一早晨自动推送文献简报。用户可以一键将新论文入库。监控主题可以手动管理。
2. **实验记录**：Alt+E唤出实验快照面板，支持截屏/终端/代码/语音四种记录类型。记录以结构化Markdown保存，支持全文检索。主窗口侧边栏有"实验"时间线。
3. **综述自动化**：用户可以从Zotero收藏夹或知识库选择文献，一键生成结构化综述草稿（含对比表格和研究空白识别）。生成过程实时展示进度。综述可保存到Obsidian。
4. **用户感知**：用户每周自动收到文献推送，做实验时可以快速记录，需要写综述时可以一键生成草稿。产品覆盖了科研工作的更多场景。

**Phase 4 → Phase 5 的衔接说明**：进入Phase 5后，数据仪表盘（功能十二）将读取Phase 0以来积累的所有活动事件数据，展示多维度的科研活动可视化。插件市场（功能九）需要所有核心API（项目、知识库、AI路由、Zotero桥、窗口检测）都稳定后才能开放接口。团队空间（功能十）需要所有数据模型都稳定后才能设计同步协议。

---


# Phase 5：平台化（约4-6周）

## 本Phase目标

将产品从"工具"升级为"平台"。这三个功能需要前面所有数据结构和API都稳定后才能可靠实现。Phase 5完成后，用户可以查看自己的科研数据仪表盘，安装社区插件扩展功能，课题组成员可以共享文献和笔记。产品从个人工具正式进化为科研协作平台。

## 前置条件

- Phase 0-4全部完成
- 所有核心功能（项目、知识库、路由、Zotero桥、写作伴侣、阅读伴侣、哨兵、实验记录、综述自动化）均已上线并稳定运行
- 事件采集系统已积累至少2-3个月的历史数据

---

## 5.1 功能十二：数据仪表盘

### 为什么先做仪表盘

事件采集系统从Phase 0就开始积累了，到Phase 5时已有至少2-3个月的历史数据，足够展示有意义的趋势分析。仪表盘的技术难度相对较低（主要是数据查询和可视化），可以快速给用户一个"全局视图"。先做它，可以让Phase 5的开局就有一个高可见度的功能交付，而不是一头扎进复杂的插件系统。

### 功能定义

这是一个帮助用户了解自己科研活动的"数据镜子"。系统以可视化的方式展示用户的科研行为数据：每天/每周/每月花了多少时间阅读文献、与AI对话了多少次、写作了多少字、哪些研究方向占用了最多时间、AI模型的使用成本和效率趋势。仪表盘不是为了监控，而是为了帮助用户发现自己的科研模式，优化时间分配。

### 界面位置

**第一处：主窗口侧边栏"仪表盘"标签页。** 与"会话""知识库""待办""实验"并列（都是Phase 1.1建立的标签体系）。点击后右侧主区域切换到仪表盘视图，以卡片+图表的形式展示数据。

**第二处：悬浮球展开面板的月度摘要。** 每月最后一天（或用户手动触发时），悬浮球展开面板顶部显示一个"本月科研回顾"入口，点击后打开一个精简版的仪表盘窗口（尺寸400x500），展示本月最关键的3-5个数字（阅读文献数、AI对话次数、写作字数、最活跃项目）。

**第三处：独立的"仪表盘"窗口（新窗口，尺寸800x600，可调整大小）。** 这个窗口提供更完整的数据视图，支持切换不同时间范围（本周/本月/本季度/本年）和不同项目筛选。窗口内布局类似Notion的仪表盘或GitHub的Profile页面，有数据卡片、折线图、饼图、热力图等多种可视化形式。

### 交互流程

**场景A：月度效率回顾**

孙同学月底打开仪表盘窗口，默认显示"本月"视图。他看到：

- 顶部一排摘要卡片：
  - "阅读23篇文献"（比上个月+5篇，趋势向上）
  - "AI对话87次"（比上个月-12次，趋势向下）
  - "写作12,400字"（比上个月+3,200字）
  - "模型消耗¥32"（比上个月-¥8，趋势向下）

- 中间的活动趋势折线图：横轴是日期，纵轴是每日活跃事件数。图中有4条不同颜色的线分别代表"阅读""对话""写作""实验"。孙同学发现自己每周三和周五的活跃度明显高于其他日子——原来这两天是他固定的"科研日"。

- 右侧的项目时间分配饼图："毕业论文"占了60%的时间，"横向课题"占25%，"通用"占15%。他意识到自己这个月对横向课题的投入不足。

- 底部的科研活跃度热力图：一个7×N的矩阵（类似GitHub的贡献图），每个格子颜色深浅代表当天的活跃程度。孙同学发现自己有两周几乎空白——那是他出差开会的日子。

- AI对话类型分布柱状图：显示87次对话中，翻译请求占了52次（60%），深度研究对话只占17次（20%），代码辅助10次，其他8次。孙同学意识到自己把太多时间花在了简单的翻译上，决定下个月多用"文献综述生成"功能来减少重复性阅读工作。

孙同学点击"导出报告"按钮，系统生成一份PDF格式的月度科研报告，他可以发给导师作为工作汇报。

**场景B：项目级仪表盘**

孙同学在仪表盘窗口顶部的项目筛选器中选择了"毕业论文"项目。视图刷新，只显示与该项目的相关数据。他看到：

- 该项目已累积阅读156篇文献，其中38篇已做笔记
- 与AI关于该项目的深度对话共43次，平均每次对话12.5轮
- 知识库中已索引89篇文献，还有31篇待索引
- 文献哨兵本月为该项目推送了14篇新论文，他阅读了其中的8篇
- 项目待办清单中已完成27项，还有5项待办

这些数据帮助孙同学直观地感受到这个项目的推进程度。

### 实现方案

**事件数据采集的回顾**：仪表盘的所有数据来源就是Phase 0建立的activity_events表。从Phase 0开始，每个功能在关键操作路径中埋入了事件记录。到Phase 5时，这张表已经积累了至少2-3个月的多维度数据，足以支撑有意义的趋势分析。

**统计查询与聚合**：原始事件数据是细粒度的（如"2026-04-23 14:32:15 打开PDF《Attention Is All You Need》第5页"），需要聚合成有意义的统计指标。聚合在本地执行，按不同维度分组：

- **时间维度**：按日/周/月/年聚合，计算每日的总活跃时长、各活动类型的次数。使用SQL的GROUP BY和COUNT/SUM函数即可实现。
- **项目维度**：按项目ID分组，计算每个项目占用的时间比例、文献增长曲线、对话深度趋势。
- **活动类型维度**：计算各活动类型的占比分布，发现用户的行为模式。
- **模型维度**：统计各AI模型的调用次数、Token消耗、成功率和成本趋势（从usage_records表聚合）。

聚合结果缓存在内存中（或使用SQLite的物化视图），当用户切换仪表盘视图时直接读取缓存，保证界面响应速度。

**可视化组件**：仪表盘使用开源图表库（如ECharts）来渲染可视化组件：

- **折线图**：展示时间趋势（每日活跃度的变化）。
- **饼图**：展示占比分布（项目时间分配）。
- **柱状图**：展示分类对比（各模型的使用量）。
- **热力图**：展示周期性模式（一周中哪几天最活跃，GitHub贡献图风格）。

所有图表支持交互——点击某个数据点可以下钻查看详细事件列表。

**月度报告导出**：系统支持将仪表盘数据导出为PDF格式的月度报告。报告模板是预先设计好的，包含封面（月份、总览数字）、各维度图表、以及AI自动生成的文字总结（"本月你最活跃的研究方向是...""建议下个月关注..."）。报告生成的过程调用AI模型（通过Phase 1.2路由器调用轻量模型）来撰写总结文字，这使得每份报告都带有一定的个性化洞察。

### 与现有系统的衔接

- **与所有功能的衔接**：仪表盘是一个"横向切片"功能，它依赖于系统中所有其他功能产生的活动事件。阅读伴侣产生"阅读"事件，AI对话产生"对话"事件，写作伴侣产生"写作"事件，实验记录产生"实验"事件，文献哨兵产生"新文献推送"事件。所有这些事件在Phase 0-4的开发中已经埋好，Phase 5只需要读取和分析。
- **与项目系统的衔接**：所有活动事件都关联到当前活跃的项目ID（Phase 1.1）。仪表盘可以按项目维度筛选数据。项目切换时，仪表盘视图自动刷新。
- **与悬浮球的衔接**：每月一次的"科研回顾"摘要通过悬浮球的红点/色点机制提醒用户。这是一个低频但高价值的触点，帮助用户建立"定期回顾"的习惯。
- **与多模型路由的衔接**：模型用量统计直接从usage_records表读取，不需要额外采集。

---

## 5.2 功能九：插件与扩展市场

### 为什么在这个位置做

插件系统需要核心API稳定（因为插件开发者会依赖这些API）。到Phase 5时，所有核心功能（项目、知识库、AI路由、Zotero桥、窗口检测）都已实现，API边界清晰，适合开放插件接口。先做仪表盘再做插件，是因为仪表盘给用户带来的"数据洞察"价值更直接，而插件系统主要面向开发者，需要前面所有功能稳定后才能设计出合理的API。

### 功能定义

这是一个开放的插件系统，允许第三方开发者为你的科研助手开发扩展功能。插件可以注册新的快捷菜单项、新的AI处理模式、与外部工具的集成、或者特定学科领域的专用功能（比如生物信息学的BLAST结果解析、化学的分子结构式渲染）。插件以JavaScript编写，运行在隔离的沙箱环境中，只能访问开发者声明的权限范围内的系统能力。

### 界面位置

**第一处：主窗口侧边栏新增"插件"标签页。** 与"会话""知识库""待办""实验""仪表盘"并列（Phase 1.1建立的标签体系）。标签页内分为两个子视图："已安装"和"发现"。"已安装"视图列出所有已安装的插件卡片（显示插件名、版本、作者、开关状态），每个卡片可以展开查看详细配置。"发现"视图展示插件市场列表（从远程registry拉取），每个插件显示简介、评分、安装次数、权限需求列表。

**第二处：设置面板新增"插件管理"卡片。** 作为插件管理的另一个入口，与侧边栏功能一致，适合习惯在设置中管理扩展的用户。

**第三处：快捷菜单（Alt+Q）的动态插件入口。** 已安装插件注册的菜单项会动态出现在Alt+Q的快捷菜单中。如果多个插件都注册了剪贴板处理菜单项，它们以子菜单或分组形式展示，每个分组上方显示插件名称作为小标题。原生功能始终在最上方，插件菜单项在下方，中间用分隔线隔开。

**第四处：AI对话中的插件触发器。** 某些插件可以在AI对话中以特殊指令形式触发。比如用户输入"/draw molecule CCO"，系统识别出"/draw"是某个化学插件注册的命令，将后续内容转交给该插件处理，插件渲染出分子结构图并插入对话流中。

### 交互流程

**场景A：安装插件**

用户打开主窗口 → 切换到"插件"标签 → 点击"发现"子视图 → 看到插件市场列表，插件按分类排列："文献工具""写作增强""学科专用""可视化""其他"。

他看到一个叫"BLAST Helper"的插件，描述是"快速处理BLAST比对结果，支持一键发送到NCBI、生成进化树、导出CSV"。点击卡片展开，看到详细信息：版本1.2，作者张三，需要权限"剪贴板读取""网络请求""结果显示窗口"。他点击"安装" → 系统从GitHub下载插件包 → 验证插件包的完整性和安全性 → 解压到本地插件目录 → 加载插件 → 弹出"安装成功，已在快捷菜单中注册3个新选项"的提示。

他打开终端，复制了一段BLAST输出结果，按Alt+Q → 快捷菜单中原有的翻译/净化项下方，新增一个分组"BLAST Helper"，里面有"发送到NCBI""生成进化树""导出CSV"三个选项。

**场景B：配置插件**

用户回到插件标签的"已安装"视图 → 找到"BLAST Helper" → 点击卡片上的齿轮图标 → 展开配置面板 → 里面是该插件定义的选项："默认数据库"（下拉选择：nt、nr、swissprot）、"结果格式"（下拉选择：Table、XML、JSON）、"自动保存路径"（文本输入框）。用户修改配置后自动保存。

**场景C：开发插件**

李四是一位生物信息学研究者，他会写JavaScript。他阅读了插件开发文档（系统内置的"开发文档"按钮），知道每个插件只需要一个manifest.json文件（描述插件基本信息、需要注册的菜单项、需要的权限）和一个main.js文件（实现菜单项的处理函数）。

他写了一个简单的插件，在main.js中注册了一个处理函数：当用户在快捷菜单中点击他的菜单项时，系统把当前剪贴板内容传给他的函数，他的函数解析BLAST文本，生成一个Markdown表格，然后通过系统提供的API调用结果显示窗口展示出来。他把这两个文件打包成zip，上传到GitHub Release。然后其他用户就可以通过插件市场搜索到并安装。

### 实现方案

**插件的生命周期**：一个插件从安装到运行经历五个阶段。

- **发现阶段**：系统从远程registry（可以是一个GitHub上的JSON文件，也可以是自托管的服务器）拉取可用插件列表。registry的格式是一个JSON数组，每个元素包含插件的ID、名称、描述、作者、版本、下载URL、图标URL、所需权限列表。
- **安装阶段**：用户点击安装后，系统下载插件包（zip格式），验证签名（可选的安全机制，比如检查文件的SHA256哈希是否与registry中声明的一致），解压到用户的本地插件目录（app_data/plugins/）。
- **加载阶段**：系统读取插件的manifest.json，验证格式和权限声明是否合法（检查权限是否在系统预定义的白名单中），然后加载插件的main.js代码。
- **运行阶段**：当用户触发插件注册的菜单项或命令时，插件的处理函数在隔离环境中执行。
- **卸载阶段**：用户点击卸载后，系统停止插件的运行，从本地目录删除插件文件，清理插件注册的所有菜单项。

**隔离执行环境**：这是插件系统的安全核心。每个插件的JavaScript代码运行在一个独立的Web Worker中（浏览器端的沙箱线程），而不是主应用的渲染线程中。Web Worker与主线程之间只能通过postMessage通信，插件无法直接访问DOM、无法直接调用系统API、也无法访问其他插件的数据。

当插件需要执行敏感操作（如读取剪贴板、调用AI模型、显示结果窗口）时，必须通过postMessage向主线程发送请求，主线程收到请求后，先检查该请求是否在插件声明的权限范围内，如果通过才代为执行。这种"代理执行"模式确保了即使插件代码有恶意行为，也无法突破权限沙箱。

**标准化API设计**：系统为插件提供一组标准化的JavaScript API，包括：

- 剪贴板读写（text和HTML格式）
- AI调用（单条生成和流式生成两种模式，通过Phase 1.2路由器）
- 结果显示（在ResultWindow中展示内容，支持Markdown和纯文本）
- 文件操作（读取用户指定的文件、写入到插件私有目录）
- 窗口通知（发送系统通知气泡）
- 本地存储（插件私有的键值对存储，用于保存配置）

这些API通过主线程的代理实现，插件代码中看起来像是直接调用，实际上都是异步的跨线程通信。

**菜单注册机制**：插件在manifest.json中声明要注册的菜单项，每个菜单项包含：ID、显示文字、图标（可选）、触发上下文（剪贴板菜单/截图菜单/全局菜单/斜杠命令）。系统启动时读取所有已启用插件的菜单声明，动态组装Alt+Q快捷菜单的内容。

**市场与分发**：插件市场可以非常轻量——本质上是托管在GitHub上的一个JSON索引文件。插件的安装来源可以是任何HTTP(S) URL，不限于官方市场，用户也可以手动安装从朋友那拿到的插件包。这种开放性类似于Zotero和Obsidian的插件生态。

### 与现有系统的衔接

- **与Alt+Q快捷菜单的衔接**：快捷菜单从静态菜单变为动态菜单。系统启动时扫描所有已启用插件的菜单声明，组装出完整的菜单结构。原生功能始终在最上方，插件注册的菜单项按插件名称分组放在下方。
- **与主窗口的衔接**：插件标签页是全新的功能模块，需要新增一个窗口视图。它复用Phase 1.1建立的侧边栏切换机制。
- **与AI对话的衔接**：插件可以注册斜杠命令（如"/draw"）。当用户在聊天输入框中输入以"/"开头的文字时，系统检测是否有插件注册了匹配的前缀命令。如果有，输入框下方弹出命令提示面板，用户确认后后续输入被路由到该插件处理。
- **与Phase 0事件采集的衔接**：每次插件被调用记录plugin_invoke事件（带上plugin_id和handler_name）。

---

## 5.3 功能十：团队科研空间

### 为什么最后做

团队空间是整个产品中最复杂的功能。它需要所有数据模型都稳定（因为它要同步所有这些数据），需要加密传输层，需要P2P或服务器架构，需要冲突解决机制。只有当个人版的所有功能都打磨成熟后，才有精力处理团队协作的复杂性。它是Phase 5的"压轴功能"，也是整个产品从"个人工具"进化为"协作平台"的最后一步。

### 功能定义

这是一个可选启用的团队协作功能。科研人员可以创建一个团队空间，邀请课题组成员加入。团队成员之间可以共享文献批注（对同一篇PDF的不同人标注）、共享笔记（共同维护一个团队Obsidian Vault或共享知识库）、共享实验记录（组内可见彼此的实验快照）、以及共享AI对话（把有价值的AI对话分享到团队空间供他人参考）。团队同步采用端到端加密，所有数据在离开用户电脑前就已经加密，即使是同步服务器也无法读取内容。

### 界面位置

**第一处：主窗口侧边栏顶部新增"团队"切换按钮。** 当用户属于至少一个团队时，侧边栏顶部项目选择器的旁边增加一个"团队"标签。点击后侧边栏切换到团队视图，显示团队列表和当前团队的动态信息流（类似一个轻量的科研Twitter，显示团队成员最近的阅读、笔记、实验活动）。

**第二处：独立的"团队空间"窗口（新窗口，尺寸480x600）。** 这个窗口展示团队空间的核心内容：成员列表（显示头像/姓名/在线状态/当前活动）、共享文献库（团队共同维护的文献集合）、共享批注（对团队文献的高亮和评论）、团队待办（课题任务分配）。

**第三处：悬浮球的团队动态提示。** 当团队成员有新的共享活动时（比如"王同学批注了《Attention Is All You Need》第3页"），悬浮球显示 subtle 的团队色点（与个人的红点区分）。悬停后展开面板顶部显示最近3条团队动态摘要。

**第四处：设置面板新增"团队协作"卡片。** 包含创建团队、加入团队（输入邀请码）、管理团队成员、配置同步模式（P2P直连还是自托管服务器）等功能。

### 交互流程

**场景A：创建团队**

张教授在设置面板点击"创建团队" → 输入团队名称"张教授课题组2026" → 选择同步模式（"设备间直连（P2P）"或"使用我的服务器"）→ 如果选择P2P，系统自动生成一个邀请码（12位字母数字组合）→ 张教授把邀请码发给5名研究生。

**场景B：加入团队**

李同学在设置面板的"团队协作"卡片中点击"加入团队" → 输入邀请码 → 系统自动验证 → 加入成功 → 主窗口侧边栏的"团队"标签激活 → 李同学点击后看到团队空间的内容：成员列表中有张教授和其他4位同学，共享文献库中已经有张教授放入的20篇经典论文。

**场景C：共享批注**

张教授在读一篇PDF时，用阅读伴侣（Phase 3.2）在第3页添加了一条批注："这个方法的关键在于负样本的选择，值得在组会上讨论"。由于他处于团队空间模式，保存批注时系统自动弹出"是否共享到团队？"选项 → 他选择"共享" → 这条批注被同步到团队空间 → 其他团队成员下次打开同一篇PDF时（通过团队共享文献库获取），在第3页看到张教授的批注气泡。

**场景D：共享AI对话**

李同学与AI进行了一段非常有价值的对话，关于实验设计的对照组选择。对话结束后，他点击主窗口的"分享到团队"按钮 → 选择"分享整个会话" → 添加备注"关于对照组设计的讨论，大家看看有没有遗漏" → 发布 → 团队动态流中新增一条："李同学分享了AI对话：实验设计讨论" → 其他成员点击后可以在自己的主窗口中查看这段对话的完整内容（但无法编辑，只能在自己的副本上继续衍生讨论）。

**场景E：团队知识库**

团队有一个共享的文献文件夹（放在组里的共享网盘上，比如实验室NAS）。所有团队成员在自己的项目设置中把这个文件夹关联为"团队文献源"。当任何成员添加新文献到该文件夹时，所有成员的知识库（Phase 1.3）自动索引这篇新文献（通过文件系统监控实现）。这意味着团队的知识库是实时同步的，任何人都可以在自己的聊天中引用团队共享的文献。

### 实现方案

**同步模式选择**：系统提供两种同步架构供团队选择。

- **P2P模式**：适用于对隐私要求极高的团队（比如涉密项目、医药研发）。在这种模式下，团队成员的设备之间直接建立加密连接（使用QUIC协议），数据不经过任何第三方服务器。P2P连接需要设备同时在线才能完成同步，但系统会缓存未同步的数据，等设备上线后自动补传。P2P模式的实现依赖libp2p或类似的P2P网络库。
- **自托管服务器模式**：适用于需要异步协作的团队。团队中的某台电脑（通常是导师的工作站或实验室服务器）运行一个轻量级的同步服务端，其他设备通过WebSocket与服务器保持连接，数据实时推送。这个服务端程序以Docker镜像形式提供，一键部署。

**端到端加密**：无论选择哪种同步模式，数据在传输前都经过端到端加密。系统为每个团队生成一个唯一的加密密钥（基于X25519椭圆曲线），团队成员加入时必须持有这个密钥才能解密数据。加密发生在发送方设备上，解密发生在接收方设备上。中间的P2P节点或自托管服务器只传递加密后的二进制数据包，无法获知内容。即使是系统开发者也无法解密用户数据。

**冲突-free数据类型（CRDT）**：团队协作文档（比如共享笔记、批注）使用CRDT数据结构来存储。CRDT是一种特殊的分布式数据结构，允许多个设备同时修改同一份数据，不需要中央服务器仲裁冲突，最终所有设备会自动收敛到一致的状态。这意味着两个团队成员同时对同一篇文献添加批注时，两份批注都会被保留，不会出现"后提交的覆盖先提交的"问题。

**成员权限与可见性**：团队空间支持三种角色：

- **Owner（创建者）**：可以管理成员、删除团队、修改所有设置。
- **Member（正式成员）**：可以读写共享内容、发布自己的内容、添加批注。
- **Viewer（访客）**：只能查看共享内容，不能发布或修改。

团队成员的在线状态通过心跳机制维护——每5分钟发送一次"我还活着"的信号，超过15分钟没有信号则显示为离线。

**活动信息流**：团队空间的核心社交组件是活动信息流（Activity Feed）。当团队成员执行特定操作时（阅读新论文、添加批注、保存实验记录、分享AI对话、完成待办），系统生成一条活动事件，加密后推送给所有在线成员。信息流按时间倒序排列，支持按成员过滤和按活动类型过滤。这不是一个实时聊天工具，而是一个"科研动态"展示板，让团队成员感知彼此的研究进展。

### 与现有系统的衔接

- **与项目系统的衔接**：团队空间是项目系统的"多用户叠加层"。每个项目可以选择是"个人项目"还是"团队项目"。团队项目的文献源、知识库、实验记录都对团队成员可见。个人项目的内容保持私密，不会同步到团队。
- **与知识库的衔接**：团队共享的文献文件夹被知识库（Phase 1.3）的文件系统监控器持续扫描。新文献到达时，所有关联该文件夹的团队成员的知识库自动索引。索引结果在本地存储，每个成员都有自己的向量数据库副本，搜索时在本地执行。
- **与阅读伴侣的衔接**：阅读伴侣（Phase 3.2）提取的批注默认保存到个人阅读笔记中，但用户可以选择"共享到团队"，此时批注通过CRDT数据结构同步到团队空间。
- **与实验记录的衔接**：实验记录（Phase 4.2）默认是个人数据，但用户在保存时可以选择"团队可见"，这样团队成员可以在"团队空间"的"实验"标签中看到这条记录。
- **与Phase 0事件采集的衔接**：团队相关的操作记录team_sync、team_share_annotation、team_share_chat等事件。

---

## Phase 5 完成状态（验收标准）

达到以下状态，Phase 5才算完成：

1. **数据仪表盘**：用户可以查看自己的科研活动数据（阅读、对话、写作、实验的时间分配），支持按项目和时间范围筛选。包含折线图、饼图、热力图等多种可视化。支持导出月度科研报告PDF。
2. **插件市场**：用户可以浏览、安装、配置、卸载插件。插件在Web Worker沙箱中运行，权限受控。第三方开发者可以通过manifest.json + main.js开发插件。Alt+Q快捷菜单动态显示插件注册的菜单项。
3. **团队空间**：用户可以创建/加入团队空间。团队成员共享文献批注、笔记、实验记录和AI对话。同步支持P2P和自托管服务器两种模式。所有数据传输端到端加密。
4. **用户感知**：用户可以回顾自己的科研数据，安装社区插件扩展功能，与课题组成员协作。产品从个人工具正式进化为科研协作平台。

**全部Phase完成后的产品状态**：AI Research Assistant已经成为一个覆盖完整科研工作流的平台——从文献发现（哨兵）、阅读（阅读伴侣）、笔记（Obsidian桥）、写作（写作伴侣+引用推荐）、实验记录（快照）、综述生成（自动化），到数据分析（仪表盘）、扩展生态（插件市场）、团队协作（团队空间）。悬浮球是这一切的统一入口。

---

# 附录：十二功能之间的协作关系总图

以上十二个功能并非孤立存在，它们构成了一个有机的科研工作流网络。以下是功能之间的关键协作链路：

**项目系统（功能四）是中心枢纽。** 所有其他功能都围绕项目运转：知识库的检索范围按项目隔离；文献哨兵的监控主题归属于项目；Zotero/Obsidian的联动配置按项目绑定；实验记录保存到项目目录；AI对话在项目上下文中进行；仪表盘的统计按项目筛选。

**知识库（功能一）是数据底座。** 它为智能引用（功能三）、阅读伴侣（功能五）、综述生成（功能八）、文献哨兵（功能二）提供底层文献检索能力。没有知识库的建立，这些功能的智能程度会大打折扣。

**AI调用层（功能十一）是动力引擎。** 所有需要AI模型的功能（聊天、润色、引用推荐、综述生成、哨兵分析、仪表盘报告）都通过多模型路由器发起调用。路由器根据任务特性自动选择最优模型，确保速度和成本的平衡。

**外部工具桥（功能三）是生态连接器。** 它把Zotero的文献管理能力和Obsidian的笔记能力引入到你的助手体系内，让用户的既有工具投资不被浪费，同时获得AI增强。

**快捷菜单与悬浮球（现有体系）是统一入口。** 无论是翻译、引用推荐、实验快照、还是插件扩展，用户的学习成本都极低——都是同一个Alt+Q或Alt+E或Alt+S触发，同一个悬浮球悬停面板展示。

**插件系统（功能九）是增长飞轮。** 当核心团队无法覆盖所有学科的特殊需求时，社区开发者可以通过插件填补空白。插件可以接入任何已有功能（在快捷菜单注册、在对话中注册斜杠命令、在结果窗口展示），形成一个正向循环：用户越多→开发者越多→插件越多→覆盖场景越广→用户越多。

**团队空间（功能十）是协作层。** 它把所有个人功能（文献、笔记、实验、对话）扩展到团队维度，让科研从一个孤独的旅程变成可以协作的过程。

**事件采集（Phase 0）是数据管线。** 从第一天开始预埋，到Phase 5的仪表盘才有数据可展示。时间维度上的提前布局，决定了后期功能的丰满程度。

---

*文档结束。请严格按照Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5的顺序依次开发。*
